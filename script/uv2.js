const {
  Document, Packer, Paragraph, TextRun, HeadingLevel, AlignmentType,
  LevelFormat, PageBreak, BorderStyle
} = require('docx');
const fs = require('fs');

const questions = [
  {
    id: 1,
    title: "1. Поняття паралельних, псевдопаралельних та розподілених обчислень",
    answer: [
      { heading: "Послідовні обчислення" },
      { text: "Класична модель обчислень — послідовна: програма виконує інструкції одну за одною на одному процесорі. Такий підхід простий у розробці та відлагодженні, але не здатний ефективно використовувати сучасне багатоядерне апаратне забезпечення." },

      { heading: "Паралельні обчислення (True Parallelism)" },
      { text: "Паралельні обчислення — це одночасне виконання кількох обчислювальних задач на кількох фізичних процесорах або ядрах процесора. Справжній паралелізм можливий лише за наявності кількох обчислювальних пристроїв (ядер, CPU, вузлів кластера). Задача розбивається на незалежні підзадачі, кожна з яких виконується на окремому ядрі одночасно з іншими." },
      { text: "Приклад: обчислення добутку двох матриць розміром N×N можна розбити по рядках — кожен потік обчислює свій рядок результуючої матриці незалежно від інших. На 8-ядерному процесорі 8 рядків обчислюються одночасно, що дає прискорення, близьке до 8x (з урахуванням накладних витрат)." },
      { text: "Паралельні обчислення застосовуються у: наукових розрахунках (моделювання клімату, молекулярна динаміка), рендерингу відео та графіки, машинному навчанні (тренування нейромереж), криптографії, обробці великих даних (Big Data)." },

      { heading: "Псевдопаралельні обчислення (Concurrency / Multitasking)" },
      { text: "Псевдопаралельні обчислення — це ілюзія одночасності на одному процесорі завдяки швидкому перемиканню між задачами (time-slicing). Операційна система виділяє кожній задачі невеликий квант часу (зазвичай 10-100 мс), після чого перемикається на наступну. Це відбувається настільки швидко, що для людини здається, ніби задачі виконуються одночасно." },
      { text: "Псевдопаралелізм вирішує задачу конкурентності (concurrency) — коли кілька задач потребують уваги процесора, але не обов'язково одночасно. Наприклад, браузер одночасно завантажує сторінку, відтворює анімацію та реагує на кліки — на однопроцесорній машині все це відбувається шляхом швидкого перемикання." },
      { text: "У Java псевдопаралелізм реалізується через потоки (Thread). Навіть якщо апаратно є лише одне ядро, можна створити кілька потоків — JVM та ОС самі вирішують, коли і скільки часу надати кожному. Перемикання між потоками потребує збереження та відновлення контексту (стек, регістри процесора, лічильник команд) — це називається context switch і є джерелом накладних витрат." },

      { heading: "Розподілені обчислення (Distributed Computing)" },
      { text: "Розподілені обчислення — це виконання задачі на кількох фізично розділених вузлах (комп'ютерах), що взаємодіють через мережу. Ключова відмінність від паралельних обчислень: вузли не мають спільної пам'яті; взаємодія відбувається виключно через обмін повідомленнями (message passing)." },
      { text: "Характеристики розподілених систем: географічна розподіленість вузлів; відсутність спільного годинника (асинхронність); можливі часткові відмови окремих вузлів; масштабування шляхом додавання нових вузлів; необхідність явного управління комунікацією." },
      { text: "Стандарт MPI (Message Passing Interface) є найпоширенішим інструментом для розподілених обчислень у наукових задачах. Java-бібліотека MPJ Express реалізує MPI для JVM. Розподілені системи також реалізуються через REST API, gRPC, Apache Kafka, Apache Spark тощо." },
      { text: "Приклад: обчислення добутку матриць у MPI — кожен процес отримує частину матриці A (певні рядки), повну матрицю B (через broadcast), виконує обчислення своєї частини та повертає результат головному процесу через Gather. Такий підхід дозволяє задіяти тисячі вузлів суперкомп'ютера." },

      { heading: "Порівняння підходів" },
      { text: "Паралельні обчислення (shared memory) мають низьку затримку доступу до даних і використовуються в межах одного вузла. Псевдопаралельні обчислення забезпечують конкурентність без реального одночасного виконання. Розподілені обчислення масштабуються практично безмежно, але потребують складнішої координації та мають вищу затримку мережевого обміну. На практиці сучасні системи поєднують усі три підходи: кластер вузлів (distributed), на кожному вузлі — кілька ядер (parallel), і безліч потоків на кожному ядрі (concurrent/pseudo-parallel)." }
    ]
  },
  {
    id: 3,
    title: "3. Способи розпаралелювання",
    answer: [
      { heading: "Декомпозиція задачі" },
      { text: "Розпаралелювання — це процес трансформації послідовного алгоритму у паралельний шляхом виявлення незалежних частин, які можна виконати одночасно. Існує два основних підходи до декомпозиції: декомпозиція за даними (data decomposition) та декомпозиція за задачами (task decomposition)." },

      { heading: "Декомпозиція за даними (Data Parallelism)" },
      { text: "Одна й та сама операція застосовується до різних підмножин даних одночасно. Це найпоширеніший тип паралелізму у наукових обчисленнях. Приклад: множення матриць. Матрицю результату можна розбити по рядках (стрічковий алгоритм) або по блоках (алгоритм Фокса / Кеннона). Кожен потік/процес опрацьовує свою частину незалежно від інших." },
      { text: "У Java декомпозиція за даними реалізується через: розподіл масивів між потоками вручну (передача діапазону індексів у Runnable/Callable); ForkJoinPool з RecursiveTask — рекурсивне розбиття задачі навпіл до досягнення порогу; Java Streams API (parallelStream()) — автоматичне розбиття колекцій; MPI Scatter/Gather — розподіл даних між процесами у розподілених системах." },

      { heading: "Декомпозиція за задачами (Task Parallelism)" },
      { text: "Різні незалежні задачі виконуються паралельно. Ці задачі можуть опрацьовувати різні дані або виконувати різні операції. Приклад: веб-сервер обробляє запити різних користувачів у різних потоках — кожен запит є окремою незалежною задачею. FixedThreadPool ідеально підходить для такого паттерну: задачі поміщаються у чергу, потоки з пулу беруть та виконують їх." },

      { heading: "Конвеєрна обробка (Pipeline Parallelism)" },
      { text: "Задача розбивається на послідовні стадії (pipeline stages), кожна з яких виконується паралельно для різних порцій даних. Аналогія: конвеєр на заводі — коли перша стадія закінчує обробку першого елемента і переходить до другого, друга стадія одночасно починає обробку першого елемента. Реалізується через паттерн Producer-Consumer: BlockingQueue між стадіями, кожна стадія — окремий пул потоків." },

      { heading: "Геометрична декомпозиція" },
      { text: "Фізичний простір або масив даних розбивається на геометричні регіони. Кожен процес/потік опрацьовує свій регіон. Використовується у моделюванні фізичних процесів (гідродинаміка, теплопровідність, молекулярна динаміка). Ключова проблема — обмін граничними даними між сусідніми регіонами після кожного кроку обчислень (halo exchange у MPI)." },

      { heading: "Рекурсивне розбиття (Divide and Conquer)" },
      { text: "Задача рекурсивно ділиться на дрібніші підзадачі до досягнення порогового розміру, після чого підзадачі вирішуються послідовно. ForkJoinFramework у Java безпосередньо реалізує цей підхід. Метод fork() запускає підзадачу асинхронно, join() очікує результату. Work-stealing алгоритм планувальника забезпечує ефективне балансування навантаження: незайнятий потік 'краде' задачі з черги зайнятого потоку." },

      { heading: "Спекулятивне розпаралелювання" },
      { text: "Потоки починають виконувати кілька гілок умовного переходу одночасно, ще не знаючи, яка з них виявиться правильною. Після визначення умови результат непотрібних гілок відкидається. Рідко використовується на практиці через складність реалізації та ризик марно витраченого процесорного часу." },

      { heading: "Вибір способу розпаралелювання" },
      { text: "Правильний вибір способу розпаралелювання критично важливий для ефективності. Якщо задача легко ділиться на однорідні частини — використовуйте декомпозицію за даними з ForkJoin або паралельними потоками. Якщо задача складається з різних незалежних роботи — декомпозиція за задачами з ThreadPool. Якщо є природний потік даних через кілька стадій обробки — конвеєр з Producer-Consumer. Необхідно враховувати гранулярність паралелізму: занадто дрібні задачі створюють надмірні накладні витрати на синхронізацію; занадто великі — призводять до незбалансованого навантаження." }
    ]
  },
  {
    id: 5,
    title: "5. Способи підвищення продуктивності комп'ютерів. Суперкомп'ютери",
    answer: [
      { heading: "Вертикальне масштабування (Scale Up)" },
      { text: "Найпростіший спосіб підвищення продуктивності — замінити наявне обладнання потужнішим: збільшити тактову частоту процесора, додати ядра, збільшити обсяг кеш-пам'яті або оперативної пам'яті. Однак закон Мура сповільнився: тактові частоти процесорів практично не ростуть з ~2005 року через теплові обмеження (power wall). Замість збільшення частоти виробники додають більше ядер (multicore) та спеціалізовані прискорювачі (GPU, TPU, FPGA)." },

      { heading: "Конвеєризація (Pipelining)" },
      { text: "Сучасні процесори розбивають виконання кожної інструкції на кілька стадій (fetch, decode, execute, write-back) та обробляють їх конвеєрно: поки одна інструкція виконується, наступна декодується, а ще наступна зчитується. Це дозволяє починати нову інструкцію щотакту, не очікуючи завершення попередньої. Процесори Intel/AMD мають конвеєр глибиною 14-20+ стадій." },

      { heading: "Суперскалярність та позапорядкове виконання" },
      { text: "Суперскалярні процесори мають кілька виконавчих пристроїв (execution units) та можуть виконувати кілька незалежних інструкцій за один такт. Out-of-order execution дозволяє процесору змінювати порядок виконання інструкцій для максимального завантаження виконавчих пристроїв, зберігаючи при цьому логічну послідовність результатів." },

      { heading: "Кеш-пам'ять" },
      { text: "Різниця у швидкості між процесором та DRAM становить сотні разів. Кеш-пам'ять (L1: ~4 цикли, L2: ~12 циклів, L3: ~30-50 циклів) зменшує цей розрив, зберігаючи часто використовувані дані поряд з процесором. Ієрархія пам'яті: регістри → L1 → L2 → L3 → DRAM → SSD/HDD. Для паралельних програм важлива cache locality: дані, що обробляються разом, мають зберігатися поруч у пам'яті." },

      { heading: "GPU як масово-паралельний прискорювач" },
      { text: "GPU (Graphics Processing Unit) містить тисячі простих ядер (наприклад, NVIDIA A100 — 6912 CUDA ядер), оптимізованих для виконання однієї операції над тисячами елементів одночасно (SIMT — Single Instruction Multiple Threads). GPU ідеальний для задач, де один алгоритм застосовується до великих масивів даних: машинне навчання, рендеринг, симуляції." },

      { heading: "Суперкомп'ютери" },
      { text: "Суперкомп'ютер — це обчислювальна система, що забезпечує продуктивність на рівні Петафлопс (10^15 операцій з плаваючою точкою за секунду) і вище. Сучасний лідер TOP500 — Frontier (Oak Ridge, США, ~1.2 ExaFLOPS). Архітектура суперкомп'ютера: масиви обчислювальних вузлів (кожен — потужний сервер з CPU + GPU); висошвидкісна мережа між вузлами (InfiniBand, до 200 Gbps); розподілена файлова система (Lustre, GPFS); масове охолодження (рідинне охолодження, занурення у діелектрик)." },
      { text: "Суперкомп'ютери використовуються для: кліматичного моделювання та прогнозу погоди; ядерних симуляцій та оборонних досліджень; розробки ліків (молекулярна динаміка); астрофізичних симуляцій; тренування великих мовних моделей (LLM). Програмування суперкомп'ютерів вимагає поєднання MPI (між вузлами), OpenMP (між ядрами вузла) та CUDA/OpenCL (для GPU)." },

      { heading: "Горизонтальне масштабування та грід-обчислення" },
      { text: "Замість одного потужного комп'ютера можна об'єднати безліч звичайних машин у кластер або грід. Грід-обчислення (Grid Computing) дозволяють об'єднувати ресурси різних організацій через інтернет. Проект SETI@home використовував мільйони домашніх ПК для аналізу радіосигналів. Хмарні обчислення (AWS, Azure, GCP) надають доступ до тисяч віртуальних машин на вимогу." }
    ]
  },
  {
    id: 7,
    title: "7. Поняття потоку обчислень. Створення та запуск потоків в Java",
    answer: [
      { heading: "Що таке потік обчислень" },
      { text: "Потік (thread) — це найменша одиниця виконання в операційній системі, яка може бути запланована планувальником ОС. Потоки є «легкими процесами»: на відміну від процесів, потоки одного процесу поділяють спільну пам'ять (heap, статичні поля, відкриті файли), але мають власний стек виконання, лічильник команд (program counter) та регістри процесора." },
      { text: "Стек потоку зберігає: локальні змінні методів; аргументи методів; адреси повернення при виклику методів; проміжні результати обчислень. Heap є спільним для всіх потоків процесу — саме звідси виникає потреба у синхронізації доступу до об'єктів." },

      { heading: "Цикл життя потоку в Java" },
      { text: "NEW — потік створений, але ще не запущений (start() не викликано). RUNNABLE — потік готовий до виконання або виконується (у Java ці стани об'єднані). BLOCKED — потік очікує захоплення монітора (synchronized блок зайнятий іншим потоком). WAITING — потік призупинений безстроково (викликано wait(), join() без таймауту). TIMED_WAITING — потік призупинений на визначений час (sleep(), wait(timeout), join(timeout)). TERMINATED — потік завершив виконання або завершився через виняток." },

      { heading: "Спосіб 1: Успадкування від Thread" },
      { text: "Перший спосіб — створити підклас Thread та перевизначити метод run():" },
      { code: `public class MyThread extends Thread {
    private final int id;
    
    public MyThread(int id) {
        this.id = id;
        setName("Worker-" + id); // задаємо ім'я потоку
    }
    
    @Override
    public void run() {
        System.out.println(getName() + " виконується у потоці: " 
            + Thread.currentThread().getName());
        // основна логіка потоку
        for (int i = 0; i < 5; i++) {
            System.out.println("Thread " + id + ": крок " + i);
        }
    }
}

// Запуск:
MyThread t1 = new MyThread(1);
MyThread t2 = new MyThread(2);
t1.start(); // ВАЖЛИВО: start(), не run()!
t2.start();` },
      { text: "Виклик start() реєструє потік в JVM та ОС і починає виконання run() у новому потоці. Виклик run() напряму — це звичайний виклик методу у поточному потоці, ніякого паралелізму не виникає. Недолік підходу: Java не підтримує множинне успадкування, тому клас, що успадковує Thread, не може успадковувати іншій клас." },

      { heading: "Спосіб 2: Реалізація інтерфейсу Runnable" },
      { text: "Рекомендований підхід — відокремити логіку задачі від механізму потоку:" },
      { code: `public class MatrixRowCalculator implements Runnable {
    private final double[][] a, b, result;
    private final int row;
    
    public MatrixRowCalculator(double[][] a, double[][] b, 
                                double[][] result, int row) {
        this.a = a; this.b = b; 
        this.result = result; this.row = row;
    }
    
    @Override
    public void run() {
        int n = b[0].length;
        int k = b.length;
        for (int j = 0; j < n; j++) {
            for (int p = 0; p < k; p++) {
                result[row][j] += a[row][p] * b[p][j];
            }
        }
    }
}

// Запуск для кожного рядка матриці:
double[][] result = new double[N][N];
Thread[] threads = new Thread[N];
for (int i = 0; i < N; i++) {
    threads[i] = new Thread(new MatrixRowCalculator(a, b, result, i));
    threads[i].start();
}
// Очікування завершення всіх потоків:
for (Thread t : threads) t.join();` },

      { heading: "Спосіб 3: Lambda та анонімні класи (Java 8+)" },
      { code: `// Lambda (найбільш лаконічний спосіб):
Thread t = new Thread(() -> {
    System.out.println("Lambda потік: " + Thread.currentThread().getName());
});
t.start();

// Через ExecutorService (рекомендовано для виробничого коду):
ExecutorService executor = Executors.newFixedThreadPool(4);
executor.submit(() -> System.out.println("Задача у пулі"));
executor.shutdown();` },

      { heading: "Спосіб 4: Callable та Future" },
      { text: "Runnable не може повертати результат і не може кидати checked exceptions. Callable вирішує обидві проблеми:" },
      { code: `Callable<Double> task = () -> {
    // важкі обчислення
    return Math.sqrt(2.0);
};

ExecutorService exec = Executors.newSingleThreadExecutor();
Future<Double> future = exec.submit(task);

// Виконання продовжується, поки задача обчислюється...
Double result = future.get(); // блокуємося до отримання результату
exec.shutdown();` }
    ]
  },
  {
    id: 9,
    title: "9. Клас Thread",
    answer: [
      { heading: "Огляд класу Thread" },
      { text: "Клас java.lang.Thread є основним механізмом управління потоками в Java. Він реалізує інтерфейс Runnable та надає повний API для створення, управління та синхронізації потоків. Кожна Java-програма має щонайменше один потік — головний потік (main thread), що запускає метод main()." },

      { heading: "Ключові конструктори" },
      { code: `Thread()                          // порожній потік
Thread(Runnable target)           // потік з задачею
Thread(Runnable target, String name) // з назвою
Thread(ThreadGroup group, Runnable target, String name) // з групою
Thread(ThreadGroup group, Runnable target, String name, long stackSize) // зі стеком` },

      { heading: "Методи управління виконанням" },
      { text: "start() — запускає потік; JVM створює новий системний потік та викликає run() в ньому. run() — метод, що виконується в новому потоці; перевизначається у підкласах або береться з Runnable. sleep(long ms) — статичний метод; призупиняє поточний потік на вказаний час, не звільняючи захоплені монітори. join() та join(long millis) — поточний потік блокується до завершення потоку, на якому викликано join(). interrupt() — встановлює прапорець переривання потоку; якщо потік заблокований у sleep/wait/join, кидається InterruptedException. isInterrupted() — повертає стан прапорця переривання, не скидаючи його. interrupted() — статичний метод; повертає та скидає прапорець переривання поточного потоку." },

      { heading: "Пріоритети потоків" },
      { code: `Thread t = new Thread(() -> {/* ... */});
t.setPriority(Thread.MAX_PRIORITY);   // 10
t.setPriority(Thread.NORM_PRIORITY);  // 5 (за замовчуванням)
t.setPriority(Thread.MIN_PRIORITY);   // 1
int p = t.getPriority();` },
      { text: "Пріоритет є підказкою планувальнику, але не гарантією. На різних ОС та JVM поведінка може відрізнятися. На Linux різниця між пріоритетами майже непомітна; на Windows вища пріоритетність помітно впливає на розподіл часу. Не варто будувати логіку програми на припущеннях щодо пріоритетів." },

      { heading: "Потоки-демони (Daemon Threads)" },
      { code: `Thread daemon = new Thread(() -> {
    while (true) {
        // фонова задача (збір метрик, heartbeat тощо)
        try { Thread.sleep(1000); } 
        catch (InterruptedException e) { break; }
    }
});
daemon.setDaemon(true); // MUST be called before start()
daemon.start();
// JVM завершується, коли всі не-демонські потоки завершились,
// незалежно від стану демонських потоків` },
      { text: "Потоки-демони використовуються для фонових сервісних задач: збирач сміття (GC), watchdog потоки, планувальники. setDaemon(true) обов'язково викликати ДО start(); після запуску змінити режим неможливо." },

      { heading: "Групи потоків (ThreadGroup)" },
      { text: "ThreadGroup дозволяє організовувати потоки в ієрархічні групи та управляти ними разом: interrupt() для всієї групи, встановлення обробника непійманих виключень, обмеження максимального пріоритету. На практиці ThreadGroup рідко використовується безпосередньо — ExecutorService надає більш сучасний та зручний API." },

      { heading: "Обробка непійманих виключень" },
      { code: `Thread t = new Thread(() -> {
    throw new RuntimeException("Помилка у потоці!");
});

// Встановлення обробника для конкретного потоку:
t.setUncaughtExceptionHandler((thread, ex) -> {
    System.err.println("Потік " + thread.getName() + 
                       " завершився з помилкою: " + ex.getMessage());
    // логування, сповіщення, перезапуск тощо
});

// Глобальний обробник для всіх потоків:
Thread.setDefaultUncaughtExceptionHandler((thread, ex) -> {
    logger.error("Необроблений виняток у " + thread.getName(), ex);
});` },

      { heading: "Корисні статичні методи" },
      { code: `Thread.currentThread()          // посилання на поточний потік
Thread.currentThread().getName() // ім'я поточного потоку
Thread.sleep(100)               // пауза 100 мс
Thread.yield()                  // підказка планувальнику поступитися ядром
Thread.activeCount()            // кількість активних потоків у групі
Thread.getAllStackTraces()       // стек-трейси всіх потоків (для діагностики)` }
    ]
  },
  {
    id: 11,
    title: "11. Стрічковий алгоритм паралельного множення матриць",
    answer: [
      { heading: "Постановка задачі" },
      { text: "Множення матриць A (розміром M×K) на матрицю B (K×N) дає матрицю C (M×N), де C[i][j] = Σ A[i][p] * B[p][j] для p від 0 до K-1. Обчислювальна складність послідовного алгоритму O(M*K*N) або O(N³) для квадратних матриць. При N=1000 це 10^9 операцій — ідеальна задача для розпаралелювання." },

      { heading: "Ідея стрічкового алгоритму" },
      { text: "Стрічковий алгоритм (Row-wise / Strip decomposition) — найпростіший підхід до паралельного множення матриць. Матриця A ділиться на горизонтальні смуги (стрічки) — групи послідовних рядків. Кожен потік/процес отримує свою стрічку матриці A та повну матрицю B, після чого самостійно обчислює відповідні рядки результуючої матриці C. Потоки/процеси не мають залежностей між собою (embarrassingly parallel), тому синхронізація потрібна лише в кінці." },

      { heading: "Реалізація з Thread/Runnable" },
      { code: `public class StripMatrixMultiply {
    static double[][] A, B, C;
    static int N;

    static class RowWorker implements Runnable {
        int startRow, endRow;
        
        RowWorker(int startRow, int endRow) {
            this.startRow = startRow;
            this.endRow = endRow;
        }
        
        @Override
        public void run() {
            for (int i = startRow; i < endRow; i++) {
                for (int j = 0; j < N; j++) {
                    C[i][j] = 0;
                    for (int k = 0; k < N; k++) {
                        C[i][j] += A[i][k] * B[k][j];
                    }
                }
            }
        }
    }

    public static double[][] multiply(double[][] a, double[][] b, int threads) 
            throws InterruptedException {
        N = a.length;
        A = a; B = b;
        C = new double[N][N];
        
        Thread[] workers = new Thread[threads];
        int rowsPerThread = N / threads;
        
        for (int t = 0; t < threads; t++) {
            int start = t * rowsPerThread;
            int end = (t == threads - 1) ? N : start + rowsPerThread;
            workers[t] = new Thread(new RowWorker(start, end));
            workers[t].start();
        }
        
        for (Thread w : workers) w.join(); // синхронна точка збору
        return C;
    }
}` },

      { heading: "Реалізація з ExecutorService та Future" },
      { code: `public static double[][] multiplyWithExecutor(double[][] a, double[][] b) 
        throws Exception {
    int n = a.length;
    double[][] c = new double[n][n];
    int cores = Runtime.getRuntime().availableProcessors();
    ExecutorService pool = Executors.newFixedThreadPool(cores);
    List<Future<?>> futures = new ArrayList<>();
    
    int rowsPerTask = Math.max(1, n / (cores * 4)); // fine-grained tasks
    
    for (int i = 0; i < n; i += rowsPerTask) {
        final int start = i;
        final int end = Math.min(i + rowsPerTask, n);
        futures.add(pool.submit(() -> {
            for (int row = start; row < end; row++)
                for (int j = 0; j < n; j++)
                    for (int k = 0; k < n; k++)
                        c[row][j] += a[row][k] * b[k][j];
        }));
    }
    
    for (Future<?> f : futures) f.get(); // очікування всіх задач
    pool.shutdown();
    return c;
}` },

      { heading: "Оптимізація: транспонування B для cache-friendly доступу" },
      { text: "Проблема: при обчисленні C[i][j] звертаємось до стовпця B[*][j] — елементи розташовані не послідовно в пам'яті (крок N double'ів між елементами). Це спричиняє багато cache miss. Вирішення: транспонувати B перед множенням. Тоді B^T[j][k] = B[k][j], і обидва рядки — i-й рядок A та j-й рядок B^T — читаються послідовно." },
      { code: `// Транспонування B:
double[][] BT = new double[n][n];
for (int i = 0; i < n; i++)
    for (int j = 0; j < n; j++)
        BT[j][i] = B[i][j];

// Множення з транспонованою матрицею:
for (int row = start; row < end; row++)
    for (int j = 0; j < n; j++) {
        double sum = 0;
        for (int k = 0; k < n; k++)
            sum += A[row][k] * BT[j][k]; // обидва рядки — послідовний доступ
        C[row][j] = sum;
    }` },

      { heading: "Аналіз ефективності" },
      { text: "Стрічковий алгоритм масштабується лінійно до кількості доступних ядер при великих матрицях. Вузьке місце — пам'ять: при N=2000 матриця займає 32 MB, що не вміщується в кеш. При використанні MPI стрічки розподіляються між процесами: кожен процес отримує свої рядки A через Scatter, повну B через Broadcast, обчислює свою частину C, і всі частини збираються через Gather." }
    ]
  },
  {
    id: 13,
    title: "13. Алгоритм Кеннона паралельного множення матриць",
    answer: [
      { heading: "Проблема класичних алгоритмів" },
      { text: "У стрічковому алгоритмі кожен процес потребує доступу до ПОВНОЇ матриці B (або A). При p процесах та матриці N×N обсяг даних, що передається, пропорційний N². Для систем із розподіленою пам'яттю (MPI-кластери) це стає вузьким місцем при великих N. Алгоритм Кеннона вирішує цю проблему, зменшуючи обсяг комунікацій." },

      { heading: "Ідея алгоритму Кеннона" },
      { text: "Алгоритм Кеннона (Cannon's Algorithm, 1969) призначений для квадратних матриць N×N на квадратній сітці √p × √p процесів. Основна ідея: замість того, щоб кожен процес мав копію всієї матриці B, матриці A і B розбиваються на блоки N/√p × N/√p, кожен блок розміщується на одному процесі, і блоки циклічно зсуваються між процесами. Таким чином кожен процес у кожному кроці отримує лише один блок (а не цілу матрицю)." },

      { heading: "Кроки алгоритму" },
      { text: "1. Ініціалізація: розмістити блоки A[i][j] та B[i][j] на процесі (i, j) сітки. Виконати початковий зсув: рядок i матриці A зсунути вліво на i позицій; стовпець j матриці B зсунути вгору на j позицій. 2. Основний цикл (√p кроків): кожен процес множить свій поточний блок A на свій поточний блок B та додає до часткового результату C[i][j]. Циклічний зсув: всі блоки A зсуваються вліво на 1; всі блоки B зсуваються вгору на 1. 3. Після √p кроків C[i][j] містить фінальний блок результуючої матриці." },

      { heading: "Реалізація в Java (схема з Thread для ілюстрації)" },
      { code: `// Спрощена ілюстрація для одного вузла
// У реальності - MPI між вузлами кластера
public class CannonAlgorithm {
    
    // Блоковий розмір
    static int blockSize;
    static int sqrtP; // корінь з кількості процесів
    
    // Початковий зсув рядків блоків A
    static double[][][] initialShiftA(double[][][] blockA) {
        double[][][] shifted = new double[sqrtP][sqrtP][];
        for (int i = 0; i < sqrtP; i++) {
            for (int j = 0; j < sqrtP; j++) {
                // Блок (i,j) отримує блок з (i, (j+i) % sqrtP)
                shifted[i][j] = blockA[i][(j + i) % sqrtP];
            }
        }
        return shifted;
    }
    
    // Множення двох блоків
    static double[] multiplyBlocks(double[] a, double[] b, int bs) {
        double[] c = new double[bs * bs];
        for (int i = 0; i < bs; i++)
            for (int j = 0; j < bs; j++)
                for (int k = 0; k < bs; k++)
                    c[i*bs + j] += a[i*bs + k] * b[k*bs + j];
        return c;
    }
    
    // Циклічний зсув A вліво на 1
    static double[][][] shiftALeft(double[][][] blockA) {
        double[][][] shifted = new double[sqrtP][sqrtP][];
        for (int i = 0; i < sqrtP; i++)
            for (int j = 0; j < sqrtP; j++)
                shifted[i][j] = blockA[i][(j + 1) % sqrtP];
        return shifted;
    }
}` },

      { heading: "Реалізація з MPI (концептуальна)" },
      { code: `// Псевдокод MPI реалізації:
// MPI_Cart_create — створення 2D топології процесів
// MPI_Cart_shift — знаходження сусідніх процесів
// MPI_Sendrecv — циклічний зсув блоків

// Кожен MPI-процес:
// 1. Отримує свій блок A та B
// 2. Виконує початковий зсув (MPI_Sendrecv по рядку/стовпцю)
// 3. Цикл sqrtP кроків:
//    a. local_C += multiply(local_A, local_B)
//    b. MPI_Sendrecv — зсув local_A вліво
//    c. MPI_Sendrecv — зсув local_B вгору
// 4. Результат local_C — частина фінальної матриці C` },

      { heading: "Порівняння з алгоритмом Фокса (Fox's Algorithm)" },
      { text: "Алгоритм Фокса (також відомий як BSP algorithm) є альтернативою Кеннону. В алгоритмі Фокса діагональний блок A[i][i] на кроці i розсилається broadcast усьому рядку i топологічної сітки, після чого всі процеси рядка множать отриманий блок на свій локальний блок B, а потім B зсувається вгору. Кеннон vs Фокс: обидва мають однакову обчислювальну складність O(N³/p); Кеннон використовує лише point-to-point комунікації (ефективніший для великих сіток); Фокс використовує broadcast у рядку (простіший у реалізації)." },

      { heading: "Складність та ефективність" },
      { text: "Обчислювальна складність: O(N³/p) на кожний процес. Комунікаційна складність: O(N²/√p) — кожен процес передає/отримує O(N²/p) даних √p разів. Прискорення: майже ідеальне (p) при достатньо великих матрицях, оскільки комунікації маскуються обчисленнями. Алгоритм Кеннона є стандартом у курсах паралельних обчислень завдяки елегантному вирішенню проблеми комунікацій." }
    ]
  },
  {
    id: 15,
    title: "15. Управління потоками в Java",
    answer: [
      { heading: "Планування потоків" },
      { text: "Планувальник потоків (thread scheduler) є частиною JVM та ОС. Він вирішує, який потік виконується в кожен момент часу. Java не гарантує конкретну стратегію планування — це залежить від JVM та ОС. На практиці використовується витісняюче планування з квантуванням часу: кожен потік виконується протягом кванту часу, після чого планувальник може переключитися на інший потік." },

      { heading: "sleep() — пауза без звільнення монітора" },
      { code: `try {
    System.out.println("Починаю паузу...");
    Thread.sleep(2000); // 2 секунди
    System.out.println("Пауза завершена");
} catch (InterruptedException e) {
    Thread.currentThread().interrupt(); // відновлюємо прапорець
    System.out.println("Потік перервано під час сну");
}` },
      { text: "sleep() не звільняє synchronized монітори! Якщо потік утримує блокування і засинає, інші потоки не можуть захопити це блокування. Починаючи з Java 11: Thread.sleep(Duration.ofSeconds(2))." },

      { heading: "join() — очікування завершення потоку" },
      { code: `Thread[] workers = new Thread[4];
for (int i = 0; i < 4; i++) {
    workers[i] = new Thread(() -> heavyComputation());
    workers[i].start();
}

// Головний потік чекає завершення всіх обчислень:
for (Thread w : workers) {
    try {
        w.join();         // без таймауту — чекає вічно
        // w.join(5000);  // з таймаутом 5 секунд
    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
    }
}
System.out.println("Всі потоки завершились");` },

      { heading: "interrupt() — м'яке переривання" },
      { code: `Thread worker = new Thread(() -> {
    while (!Thread.currentThread().isInterrupted()) {
        try {
            // перевіряємо прапорець у довгих циклах
            processNextItem();
            Thread.sleep(100);
        } catch (InterruptedException e) {
            // sleep/wait скидає прапорець, треба відновити
            Thread.currentThread().interrupt();
            break; // виходимо з циклу
        }
    }
    System.out.println("Потік завершено коректно");
});

worker.start();
Thread.sleep(3000);
worker.interrupt(); // просимо потік зупинитися
worker.join();` },
      { text: "interrupt() не зупиняє потік примусово — це лише сигнал. Потік має самостійно перевіряти прапорець і вирішувати, як реагувати. Це дозволяє коректно завершити поточну операцію та вивільнити ресурси." },

      { heading: "yield() — добровільна поступка ядром" },
      { text: "Thread.yield() підказує планувальнику, що поточний потік готовий поступитися процесорним часом на користь інших потоків такого ж або вищого пріоритету. Поведінка не гарантована і залежить від реалізації JVM. Використовується у spin-lock реалізаціях для зменшення marnotrawstwa ядра при активному очікуванні." },

      { heading: "Стан та моніторинг потоків" },
      { code: `Thread t = new Thread(() -> { /* ... */ });
System.out.println(t.getState());    // NEW
t.start();
System.out.println(t.getState());    // RUNNABLE або TIMED_WAITING
System.out.println(t.isAlive());     // true поки не завершився
System.out.println(t.isDaemon());
System.out.println(t.getId());       // унікальний ID потоку
System.out.println(t.getName());

// Дамп всіх потоків (корисно для діагностики deadlock):
Map<Thread, StackTraceElement[]> allStacks = Thread.getAllStackTraces();
for (Map.Entry<Thread, StackTraceElement[]> entry : allStacks.entrySet()) {
    System.out.println("Потік: " + entry.getKey().getName() 
                       + " [" + entry.getKey().getState() + "]");
}` },

      { heading: "Завершення потоків: застарілі та сучасні методи" },
      { text: "Методи stop(), suspend(), resume() є застарілими (deprecated) та небезпечними: stop() може залишити об'єкти в неконсистентному стані, звільняючи монітори у випадковий момент; suspend() може призвести до deadlock, якщо призупинений потік тримає монітор. Сучасний підхід: використовувати volatile булеву змінну-прапорець або AtomicBoolean для управління завершенням, або переривання через interrupt()." }
    ]
  },
  {
    id: 17,
    title: "17. Блокування потоку",
    answer: [
      { heading: "Монітор та ключове слово synchronized" },
      { text: "Кожен об'єкт в Java має прихований монітор (mutex). Ключове слово synchronized дозволяє лише одному потоку одночасно виконувати захищений блок коду або метод. Решта потоків, що намагаються увійти у synchronized блок/метод, переходять у стан BLOCKED та чекають звільнення монітора." },
      { code: `public class Counter {
    private int count = 0;
    
    // Синхронізований метод — монітор this
    public synchronized void increment() {
        count++; // read-modify-write — не атомарно без sync!
    }
    
    // Еквівалентний synchronized блок:
    public void decrement() {
        synchronized (this) {
            count--;
        }
    }
    
    // Синхронізація на окремому об'єкті-локері:
    private final Object lock = new Object();
    public void reset() {
        synchronized (lock) {
            count = 0;
        }
    }
}` },

      { heading: "ReentrantLock — явне блокування" },
      { text: "java.util.concurrent.locks.ReentrantLock надає більш гнучкий механізм блокування порівняно з synchronized:" },
      { code: `ReentrantLock lock = new ReentrantLock();

public void safeMethod() {
    lock.lock();
    try {
        // критична секція
        sharedData.update();
    } finally {
        lock.unlock(); // ЗАВЖДИ у finally блоці!
    }
}

// Спроба захопити блокування з таймаутом:
public boolean tryUpdate() {
    try {
        if (lock.tryLock(100, TimeUnit.MILLISECONDS)) {
            try {
                sharedData.update();
                return true;
            } finally {
                lock.unlock();
            }
        }
        return false; // не вдалося захопити
    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
        return false;
    }
}` },

      { heading: "ReadWriteLock — оптимізація для читачів" },
      { code: `ReadWriteLock rwLock = new ReentrantReadWriteLock();

// Багато читачів можуть читати одночасно:
public String read() {
    rwLock.readLock().lock();
    try {
        return sharedData.get();
    } finally {
        rwLock.readLock().unlock();
    }
}

// Лише один записувач, виключає всіх читачів:
public void write(String data) {
    rwLock.writeLock().lock();
    try {
        sharedData.set(data);
    } finally {
        rwLock.writeLock().unlock();
    }
}` },
      { text: "ReadWriteLock ефективний коли читань набагато більше, ніж записів (наприклад, конфігурація, кеш). Дозволяє паралельне читання, але ексклюзивний запис." },

      { heading: "Deadlock — взаємне блокування" },
      { text: "Deadlock виникає, коли два або більше потоки взаємно очікують один одного, утримуючи ресурси, які потрібні іншим. Умови виникнення deadlock (умови Коффмана): взаємне виключення; утримання та очікування; неможливість примусового вилучення ресурсу; кругове очікування." },
      { code: `// Класичний приклад deadlock:
Object lockA = new Object();
Object lockB = new Object();

Thread t1 = new Thread(() -> {
    synchronized (lockA) {          // T1 захоплює A
        Thread.sleep(100);
        synchronized (lockB) { }    // T1 чекає B (B тримає T2)
    }
});

Thread t2 = new Thread(() -> {
    synchronized (lockB) {          // T2 захоплює B
        Thread.sleep(100);
        synchronized (lockA) { }    // T2 чекає A (A тримає T1)
    }
});` },
      { text: "Рішення: завжди захоплювати блокування в одному порядку; використовувати tryLock() з таймаутом; мінімізувати кількість одночасно захоплених блокувань; використовувати Lock Ordering — упорядковувати захоплення локерів за ID об'єкта." },

      { heading: "Starvation та Livelock" },
      { text: "Starvation (голодування) — потік ніколи не отримує ресурс через несправедливий планувальник або постійну зайнятість ресурсу іншими потоками. Рішення: ReentrantLock(true) — fair lock, дає перевагу потокам, що чекають найдовше. Livelock — потоки активно змінюють стан у відповідь один на одного, але не просуваються. Аналогія: два ввічливі люди, що одночасно поступаються дорогою і рухаються в той самий бік." }
    ]
  },
  {
    id: 19,
    title: "19. Синхронізовані методи",
    answer: [
      { heading: "Потреба у синхронізації" },
      { text: "Без синхронізації паралельний доступ до спільних змінних призводить до race condition — стану гонитви. Класичний приклад: два потоки виконують count++ одночасно. Операція count++ розкладається на три кроки: read (завантажити значення count), modify (+1), write (записати назад). Якщо два потоки виконують ці три кроки паралельно, кінцевий результат може бути меншим за очікуваний — одне з оновлень «губиться»." },

      { heading: "synchronized методи" },
      { code: `public class BankAccount {
    private double balance;
    
    // монітор = this (об'єкт BankAccount)
    public synchronized void deposit(double amount) {
        if (amount <= 0) throw new IllegalArgumentException();
        balance += amount;
        notifyAll(); // повідомляємо потоки, що чекають на баланс
    }
    
    public synchronized void withdraw(double amount) 
            throws InterruptedException {
        while (balance < amount) {
            wait(); // відпускає монітор і чекає notify
        }
        balance -= amount;
    }
    
    public synchronized double getBalance() {
        return balance;
    }
}` },

      { heading: "wait(), notify(), notifyAll()" },
      { text: "wait() — поточний потік відпускає монітор і переходить у WAITING. Важливо: wait() ПОВИНЕН викликатись у synchronized блоці; wait() може прокинутися помилково (spurious wakeup), тому завжди використовуйте while, а не if. notify() — будить один довільний потік, що чекає на цьому моніторі. notifyAll() — будить усі потоки, що чекають. Зазвичай безпечніше використовувати notifyAll()." },
      { code: `// ПРАВИЛЬНИЙ паттерн wait/notify:
synchronized (lock) {
    while (!conditionMet()) { // while, не if!
        lock.wait();
    }
    // умова виконана, продовжуємо
}

// Сигналізація:
synchronized (lock) {
    condition = true;
    lock.notifyAll();
}` },

      { heading: "volatile — видимість без взаємного виключення" },
      { code: `public class FlagExample {
    private volatile boolean running = true;
    
    public void stop() {
        running = false; // видимо всім потокам негайно
    }
    
    public void run() {
        while (running) { // читання завжди з головної пам'яті
            doWork();
        }
    }
}` },
      { text: "volatile гарантує: видимість (зміна одним потоком одразу видима іншим); порядок (операції до запису volatile видимі після читання volatile). volatile НЕ гарантує атомарність складених операцій (count++ НЕ є безпечним з volatile)." },

      { heading: "Атомарні класи (java.util.concurrent.atomic)" },
      { code: `AtomicInteger counter = new AtomicInteger(0);

// Атомарні операції (без synchronized):
counter.incrementAndGet();    // ++counter
counter.getAndIncrement();    // counter++
counter.addAndGet(5);         // counter += 5
counter.compareAndSet(10, 20); // якщо == 10, встановити 20

// AtomicReference для об'єктів:
AtomicReference<String> ref = new AtomicReference<>("initial");
ref.compareAndSet("initial", "updated"); // CAS операція

// LongAdder — ефективніший лічильник при великому числі потоків:
LongAdder adder = new LongAdder();
adder.increment();
long total = adder.sum();` },
      { text: "Атомарні класи реалізовані через CAS (Compare-And-Swap) — апаратну інструкцію процесора. Це lock-free підхід, що забезпечує прогрес (один з потоків завжди просувається), на відміну від mutex, де всі заблоковані крім одного." },

      { heading: "Happens-Before (модель пам'яті Java)" },
      { text: "Java Memory Model (JMM) визначає правила видимості змін між потоками через відношення happens-before. Гарантії happens-before: запуск потоку (start()) happens-before першої дії потоку; запис у поле до завершення потоку (join()) happens-before читання після join(); unlock монітора happens-before lock того ж монітора; запис volatile happens-before читання того ж volatile поля. Без happens-before відношення між операціями JVM може переупорядковувати їх (memory reordering) — і це є коректно з точки зору JMM." }
    ]
  },
  {
    id: 21,
    title: "21. Високорівневі способи управління потоками з використанням бібліотеки java.util.concurrent",
    answer: [
      { heading: "Огляд java.util.concurrent (JUC)" },
      { text: "Пакет java.util.concurrent, введений у Java 5 (JSR-166), надає потужний набір інструментів для конкурентного програмування, що значно простіші та безпечніші за пряме використання Thread та synchronized. Архітектор — Дуг Ліа (Doug Lea). Основні компоненти: пули потоків (Executor Framework); синхронізатори (Semaphore, CountDownLatch, CyclicBarrier, Phaser, Exchanger); конкурентні колекції (ConcurrentHashMap, CopyOnWriteArrayList, BlockingQueue); атомарні змінні; блокування (Lock, ReadWriteLock)." },

      { heading: "Executor Framework" },
      { code: `// ExecutorService — базовий інтерфейс для пулів потоків
ExecutorService executor = Executors.newFixedThreadPool(4);

// submit повертає Future — можна отримати результат:
Future<Integer> future = executor.submit(() -> {
    return expensiveComputation();
});

// Не блокуємось — продовжуємо паралельну роботу
doOtherWork();

// Тепер отримуємо результат (блокуємось якщо ще не готовий):
Integer result = future.get(5, TimeUnit.SECONDS); // з таймаутом
executor.shutdown();
executor.awaitTermination(10, TimeUnit.SECONDS);` },

      { heading: "CompletableFuture — асинхронне програмування" },
      { code: `// Ланцюжок асинхронних операцій:
CompletableFuture<String> cf = CompletableFuture
    .supplyAsync(() -> fetchDataFromDB())          // async операція
    .thenApply(data -> processData(data))          // трансформація
    .thenApply(result -> formatResult(result))     // ще одна
    .exceptionally(ex -> "Помилка: " + ex.getMessage()); // обробка помилок

// Комбінування кількох futures:
CompletableFuture<String> f1 = CompletableFuture.supplyAsync(() -> "Hello");
CompletableFuture<String> f2 = CompletableFuture.supplyAsync(() -> "World");

CompletableFuture<String> combined = f1.thenCombine(f2, 
    (s1, s2) -> s1 + " " + s2);

// Очікування всіх:
CompletableFuture.allOf(f1, f2, combined).join();` },

      { heading: "CountDownLatch — одноразовий бар'єр" },
      { code: `CountDownLatch startSignal = new CountDownLatch(1);
CountDownLatch doneSignal = new CountDownLatch(5);

for (int i = 0; i < 5; i++) {
    executor.submit(() -> {
        try {
            startSignal.await(); // всі чекають старту
            doWork();
        } finally {
            doneSignal.countDown(); // сигналізуємо про завершення
        }
    });
}

startSignal.countDown(); // дозволяємо всім стартувати разом
doneSignal.await();      // чекаємо завершення всіх` },

      { heading: "CyclicBarrier — багаторазовий бар'єр" },
      { code: `// На відміну від CountDownLatch — може бути скинутий і використаний знову
CyclicBarrier barrier = new CyclicBarrier(4, () -> {
    System.out.println("Всі потоки досягли бар'єру, продовжуємо");
    // дія виконується коли всі прийшли
});

for (int i = 0; i < 4; i++) {
    executor.submit(() -> {
        for (int step = 0; step < 3; step++) {
            computeStep(step);
            barrier.await(); // чекаємо всіх на кожному кроці
        }
    });
}` },
      { text: "CyclicBarrier корисний для ітеративних алгоритмів, де кожна ітерація потребує синхронізації між потоками (наприклад, обмін граничними значеннями у стрічковому алгоритмі)." },

      { heading: "Semaphore — обмеження конкурентного доступу" },
      { code: `// Обмежуємо кількість одночасних підключень до БД:
Semaphore dbConnections = new Semaphore(10);

public void queryDatabase() {
    try {
        dbConnections.acquire(); // захоплюємо один дозвіл
        try {
            executeQuery();
        } finally {
            dbConnections.release(); // завжди звільняємо
        }
    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
    }
}` },

      { heading: "Phaser — гнучкий синхронізатор фаз" },
      { text: "Phaser — найгнучкіший синхронізатор, що комбінує функціональність CountDownLatch та CyclicBarrier. Дозволяє динамічно реєструвати/скасовувати реєстрацію учасників. Корисний для алгоритмів зі змінною кількістю активних потоків на кожній фазі." }
    ]
  },
  {
    id: 23,
    title: "23. Пули потоків",
    answer: [
      { heading: "Навіщо потрібні пули потоків" },
      { text: "Створення нового потоку є дорогою операцією: JVM виділяє пам'ять для стеку (512KB–1MB за замовчуванням), ОС реєструє системний потік, і відбувається переключення контексту. При обробці тисяч короткочасних задач (HTTP-запити, задачі з черги) постійне створення/знищення потоків стає вузьким місцем. Пул потоків вирішує цю проблему: потоки створюються заздалегідь, отримують задачі з черги, і після виконання задачі повертаються в пул та чекають наступної задачі." },

      { heading: "ThreadPoolExecutor — основа всіх пулів" },
      { code: `// Повний конструктор з усіма параметрами:
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    4,                              // corePoolSize: мін. кількість потоків
    16,                             // maximumPoolSize: макс. кількість потоків
    60L, TimeUnit.SECONDS,          // keepAliveTime: TTL для надлишкових потоків
    new LinkedBlockingQueue<>(100), // workQueue: черга задач
    new ThreadFactory() {           // ThreadFactory (можна кастомізувати)
        int count = 0;
        public Thread newThread(Runnable r) {
            Thread t = new Thread(r, "Worker-" + count++);
            t.setDaemon(false);
            return t;
        }
    },
    new ThreadPoolExecutor.CallerRunsPolicy() // RejectedExecutionHandler
);` },

      { heading: "Стратегії відхилення задач (RejectedExecutionHandler)" },
      { text: "AbortPolicy (за замовчуванням): кидає RejectedExecutionException. CallerRunsPolicy: задача виконується у потоці, що її надсилає — це природньо обмежує швидкість надходження задач. DiscardPolicy: задача мовчки відкидається. DiscardOldestPolicy: найстаріша задача в черзі відкидається, нова ставиться на її місце." },

      { heading: "Фабричні методи Executors" },
      { code: `// Фіксований пул — підходить для CPU-інтенсивних задач:
ExecutorService fixed = Executors.newFixedThreadPool(
    Runtime.getRuntime().availableProcessors()
);

// Кеширований пул — для великої кількості короткочасних задач:
// потоки створюються на вимогу, виживають 60с без задач
ExecutorService cached = Executors.newCachedThreadPool();

// Один потік — гарантує послідовне виконання задач:
ExecutorService single = Executors.newSingleThreadExecutor();

// Планувальник — для задач за розкладом:
ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(2);
scheduler.scheduleAtFixedRate(() -> collectMetrics(), 0, 5, TimeUnit.SECONDS);
scheduler.scheduleWithFixedDelay(() -> cleanup(), 1, 10, TimeUnit.SECONDS);` },

      { heading: "FixedThreadPool для імітації СМО (Системи масового обслуговування)" },
      { text: "Система масового обслуговування (СМО) моделює черги обслуговування: клієнти (заявки) надходять з певною інтенсивністю λ, обслуговуються серверами (каналами) з інтенсивністю μ. FixedThreadPool ідеально моделює багатоканальну СМО: потоки пулу = канали обслуговування; черга BlockingQueue = черга очікування; Submit() = надходження заявки." },
      { code: `// Імітація СМО M/M/c (Erlang C):
public class QueuingSystemSimulation {
    static final int NUM_SERVERS = 4;      // кількість каналів
    static final int QUEUE_CAPACITY = 20;  // місткість черги
    
    public static void simulate() throws InterruptedException {
        BlockingQueue<Runnable> queue = 
            new LinkedBlockingQueue<>(QUEUE_CAPACITY);
        ThreadPoolExecutor pool = new ThreadPoolExecutor(
            NUM_SERVERS, NUM_SERVERS, 0L, TimeUnit.MILLISECONDS, queue,
            new ThreadPoolExecutor.DiscardPolicy() // відмова при переповненні
        );
        
        AtomicLong served = new AtomicLong(0);
        AtomicLong rejected = new AtomicLong(0);
        
        // Генерація заявок (Пуасонівський потік):
        Random rng = new Random();
        for (int i = 0; i < 1000; i++) {
            Thread.sleep((long)(rng.nextExponential() * 100)); // λ = 10/с
            try {
                pool.execute(() -> {
                    try {
                        long serviceTime = (long)(rng.nextExponential() * 300);
                        Thread.sleep(serviceTime); // μ = 3.3/с
                        served.incrementAndGet();
                    } catch (InterruptedException e) {
                        Thread.currentThread().interrupt();
                    }
                });
            } catch (RejectedExecutionException e) {
                rejected.incrementAndGet(); // заявка відхилена (черга повна)
            }
        }
        pool.shutdown();
        pool.awaitTermination(60, TimeUnit.SECONDS);
        System.out.printf("Обслуговано: %d, відхилено: %d%n", 
            served.get(), rejected.get());
    }
}` },

      { heading: "Моніторинг та налаштування пулу" },
      { code: `ThreadPoolExecutor tpe = (ThreadPoolExecutor) executor;
System.out.println("Активних потоків: " + tpe.getActiveCount());
System.out.println("Потоків у пулі: " + tpe.getPoolSize());
System.out.println("Задач у черзі: " + tpe.getQueue().size());
System.out.println("Виконано задач: " + tpe.getCompletedTaskCount());` },
      { text: "Правило вибору розміру пулу: для CPU-bound задач — кількість ядер + 1 (одне запасне на IO блокування); для IO-bound задач — більший пул, наприклад кількість ядер × (1 + очікуваний час IO / час CPU)." }
    ]
  },
  {
    id: 25,
    title: "25. Розробка високоефективних паралельних програм з використанням ForkJoinFramework",
    answer: [
      { heading: "Концепція ForkJoin" },
      { text: "ForkJoinFramework (Java 7+) реалізує парадигму divide-and-conquer для паралельних обчислень. Задача рекурсивно ділиться (fork) на менші підзадачі до досягнення порогового розміру, вирішується послідовно, а результати об'єднуються (join). Ключова відмінність від ThreadPoolExecutor: work-stealing алгоритм — кожен потік має власну двосторонню чергу задач; незайнятий потік 'краде' задачі з хвоста черги зайнятого потоку; це забезпечує відмінний баланс навантаження без централізованої черги." },

      { heading: "ForkJoinPool" },
      { code: `// Спільний пул (рекомендується для більшості задач):
ForkJoinPool commonPool = ForkJoinPool.commonPool();
// розмір = кількість доступних процесорів - 1

// Власний пул (для ізоляції або специфічного паралелізму):
ForkJoinPool customPool = new ForkJoinPool(
    8,                              // parallelism
    ForkJoinPool.defaultForkJoinWorkerThreadFactory,
    null,                           // UncaughtExceptionHandler
    false                           // asyncMode
);` },

      { heading: "RecursiveTask — задача з результатом" },
      { code: `public class ParallelSum extends RecursiveTask<Long> {
    static final int THRESHOLD = 10_000; // поріг для послідовного виконання
    private final long[] array;
    private final int start, end;
    
    public ParallelSum(long[] array, int start, int end) {
        this.array = array; this.start = start; this.end = end;
    }
    
    @Override
    protected Long compute() {
        int length = end - start;
        if (length <= THRESHOLD) {
            // Базовий випадок: послідовне обчислення
            long sum = 0;
            for (int i = start; i < end; i++) sum += array[i];
            return sum;
        }
        
        int mid = start + length / 2;
        // Розщеплення на дві підзадачі:
        ParallelSum leftTask = new ParallelSum(array, start, mid);
        ParallelSum rightTask = new ParallelSum(array, mid, end);
        
        leftTask.fork();            // async запуск лівої підзадачі
        long rightResult = rightTask.compute(); // синхронне виконання правої
        long leftResult = leftTask.join();      // очікуємо лівої
        
        return leftResult + rightResult;
    }
    
    public static void main(String[] args) {
        long[] data = new long[1_000_000];
        // ... заповнення масиву ...
        
        ForkJoinPool pool = ForkJoinPool.commonPool();
        long sum = pool.invoke(new ParallelSum(data, 0, data.length));
        System.out.println("Сума: " + sum);
    }
}` },

      { heading: "RecursiveAction — задача без результату" },
      { code: `public class ParallelMergeSort extends RecursiveAction {
    static final int THRESHOLD = 1000;
    private final int[] array;
    private final int start, end;
    
    public ParallelMergeSort(int[] array, int start, int end) {
        this.array = array; this.start = start; this.end = end;
    }
    
    @Override
    protected void compute() {
        if (end - start <= THRESHOLD) {
            Arrays.sort(array, start, end); // послідовне сортування
            return;
        }
        int mid = (start + end) / 2;
        
        // Паралельне сортування обох половин:
        invokeAll(
            new ParallelMergeSort(array, start, mid),
            new ParallelMergeSort(array, mid, end)
        );
        
        merge(array, start, mid, end); // злиття (послідовно)
    }
}` },

      { heading: "Пошук у групі файлів методом ForkJoin" },
      { code: `public class FileSearchTask extends RecursiveTask<List<Path>> {
    private final Path directory;
    private final String searchPattern;
    
    public FileSearchTask(Path directory, String searchPattern) {
        this.directory = directory;
        this.searchPattern = searchPattern;
    }
    
    @Override
    protected List<Path> compute() {
        List<Path> results = new ArrayList<>();
        List<FileSearchTask> subTasks = new ArrayList<>();
        
        try (DirectoryStream<Path> stream = 
                Files.newDirectoryStream(directory)) {
            for (Path entry : stream) {
                if (Files.isDirectory(entry)) {
                    FileSearchTask subTask = 
                        new FileSearchTask(entry, searchPattern);
                    subTask.fork();         // паралельно обходимо піддиректорії
                    subTasks.add(subTask);
                } else if (matchesPattern(entry)) {
                    results.add(entry);
                }
            }
        } catch (IOException e) {
            System.err.println("Помилка доступу: " + directory);
        }
        
        // Збираємо результати від підзадач:
        for (FileSearchTask task : subTasks) {
            results.addAll(task.join());
        }
        return results;
    }
    
    private boolean matchesPattern(Path file) {
        return file.getFileName().toString().contains(searchPattern);
    }
    
    public static void main(String[] args) throws Exception {
        Path root = Paths.get("/home/user/documents");
        ForkJoinPool pool = ForkJoinPool.commonPool();
        List<Path> found = pool.invoke(new FileSearchTask(root, ".java"));
        found.forEach(System.out::println);
    }
}` },

      { heading: "Оптимізація ForkJoin: вибір порогу" },
      { text: "Поріг (THRESHOLD) є критичним параметром. Занадто малий поріг → надмірні накладні витрати на fork/join (кожен fork — це синхронізація, виділення пам'яті). Занадто великий поріг → недостатнє розпаралелювання, деякі потоки простоюють. Практичне правило: поріг = N / (parallelism * 4). Для масивів: зазвичай 1000–10000 елементів. Обов'язково профілюйте конкретну задачу!" }
    ]
  },
  {
    id: 27,
    title: "27. Модель обчислень у вигляді графа «операції-операнди»",
    answer: [
      { heading: "Граф залежностей обчислень" },
      { text: "Модель «операції-операнди» (operation-operand graph, або граф залежностей, DAG — Directed Acyclic Graph) є фундаментальним інструментом аналізу паралелізму в алгоритмах. У цьому графі: вузли (nodes) представляють операції (обчислення, присвоєння, порівняння); ребра (edges) представляють залежності за даними — ребро з Op1 до Op2 означає, що Op2 потребує результату Op1. Граф є ациклічним, оскільки циклічні залежності означали б нескінченну рекурсію." },
      { text: "Аналіз графу залежностей дозволяє відповісти на питання: які операції можна виконати паралельно? Яка мінімально можлива кількість кроків (критичний шлях)? Скільки процесорів насправді потрібно?" },

      { heading: "Побудова графу залежностей" },
      { text: "Для кожного прикладу: P1: a = 2; P2: b = 3; P3: c = a + b; P4: d = a * 2; P5: e = c + d. Граф залежностей: P1 → P3, P1 → P4, P2 → P3, P3 → P5, P4 → P5. Операції P1 і P2 незалежні — можна виконати паралельно. P3 і P4 залежать від P1 (і P3 ще від P2) — починають після P1 та P2 — теж можна паралельно. P5 залежить від обох P3 і P4 — виконується останньою." },
      { text: "Критичний шлях (critical path): найдовший ланцюжок залежностей у графі. Це теоретичний мінімум часу виконання навіть при нескінченній кількості процесорів. У прикладі: P1 → P3 → P5 або P1 → P4 → P5 — обидва шляхи довжиною 3 — критичний шлях = 3 кроки." },

      { heading: "Визначення ступеня паралелізму" },
      { text: "Ширина (width) графу на рівні i — кількість вузлів на i-му рівні (всі операції, до яких веде критичний шлях довжиною i). Максимальна ширина — верхня межа кількості корисних процесорів (лінійний закон Берента: час ≥ критичний шлях і час ≥ загальна робота / кількість процесорів)." },

      { heading: "Застосування в Java ForkJoin" },
      { text: "ForkJoinFramework неявно будує граф залежностей: кожен fork() породжує нову гілку графу; join() встановлює залежність між батьківською задачею та дочірньою. Work-stealing планувальник намагається виконувати вузли графу, що не мають незадоволених залежностей (готові до виконання), максимально ефективно, не блокуючи потоки в очікуванні." },

      { heading: "Граф для множення матриць" },
      { text: "Для матриць N×N граф операцій множення: N² вузлів-скалярних добутків (обчислення одного елемента C[i][j] = Σ A[i][k]*B[k][j]) — всі незалежні між собою (нема залежностей між різними (i,j)). Це означає ступінь паралелізму N² — всі N² елементів можна обчислювати одночасно. Критичний шлях: N операцій множення + log₂N операцій додавання (за деревом підсум). При N=1000 критичний шлях ≈ 1010, а загальне число операцій ≈ 10⁹ — потенційне прискорення ≈ 10⁶x." }
    ]
  },
  {
    id: 29,
    title: "29. Показники ефективності паралельного алгоритму",
    answer: [
      { heading: "Основні метрики" },
      { text: "Оцінка ефективності паралельного алгоритму потребує кількісних показників. Позначення: T₁ — час виконання послідовного алгоритму; Tₚ — час виконання паралельного алгоритму на p процесорах; p — кількість процесорів." },

      { heading: "Прискорення (Speedup)" },
      { text: "Прискорення Sₚ = T₁ / Tₚ. Показує у скільки разів паралельний алгоритм швидший за послідовний. Ідеальне (лінійне) прискорення: Sₚ = p (кожен з p процесорів виконує рівно 1/p роботи). На практиці Sₚ < p через накладні витрати синхронізації, комунікацій та нерівномірний розподіл навантаження. Суперлінійне прискорення (Sₚ > p) можливе, коли задача не вміщується в кеш одного процесора, але вміщується в сумарний кеш p процесорів." },

      { heading: "Ефективність (Efficiency)" },
      { text: "Ефективність Eₚ = Sₚ / p = T₁ / (p × Tₚ). Показує, яку частку свого часу кожен процесор корисно використовує (0 < Eₚ ≤ 1, або 0%–100%). Eₚ = 1 (100%) — ідеальний випадок, всі процесори завантажені весь час. Eₚ < 1 — частина процесорного часу витрачається на очікування, синхронізацію, комунікації. Зазвичай при збільшенні p ефективність падає." },

      { heading: "Вартість (Cost) та Надлишковість (Overhead)" },
      { text: "Вартість паралельного алгоритму: Cₚ = p × Tₚ — загальний процесорний час, витрачений на розв'язання задачі. Надлишковість (overhead): O = Cₚ - T₁ = p × Tₚ - T₁ — час, витрачений понад мінімально необхідний. Алгоритм називається оптимальним (cost-optimal) якщо Cₚ = O(T₁), тобто вартість не перевищує послідовний час за порядком величини." },

      { heading: "Масштабованість (Scalability)" },
      { text: "Масштабованість — здатність алгоритму підтримувати прийнятну ефективність при зростанні кількості процесорів. Сильна масштабованість (strong scaling): розмір задачі фіксований, збільшуємо p — ефективно, якщо прискорення зростає близько лінійно. Слабка масштабованість (weak scaling): збільшуємо p та розмір задачі пропорційно — ефективно, якщо час виконання залишається приблизно постійним. Ізоефективність (isoefficiency function) W(p): мінімальний розмір задачі W, при якому ефективність залишається на рівні E при p процесорах." },

      { heading: "Приклад розрахунку" },
      { text: "Множення матриць 1000×1000 на 8 ядрах: T₁ = 8 секунд (послідовно); T₈ = 1.2 секунди (паралельно). Прискорення S₈ = 8 / 1.2 ≈ 6.67. Ефективність E₈ = 6.67 / 8 ≈ 83%. Вартість C₈ = 8 × 1.2 = 9.6 с. Накладні витрати O = 9.6 - 8 = 1.6 с. Висновок: 83% ефективність є хорошим результатом; 17% втрат на синхронізацію та нерівномірне балансування." }
    ]
  },
  {
    id: 31,
    title: "31. Закон Амдала. Ефект Амдала",
    answer: [
      { heading: "Формулювання закону Амдала" },
      { text: "Закон Амдала (Gene Amdahl, 1967) визначає теоретичну максимальну межу прискорення паралельної програми. Нехай f — частка програми, яку не можна розпаралелити (послідовна частина), 0 ≤ f ≤ 1. Тоді прискорення на p процесорах: Sₚ = 1 / (f + (1-f)/p). При p→∞: S∞ = 1/f — абсолютна межа прискорення." },

      { heading: "Інтерпретація" },
      { text: "Приклад 1: f = 0.1 (10% коду є послідовним). S∞ = 1/0.1 = 10. Тобто навіть з нескінченною кількістю процесорів прискорення не перевищить 10x. На 100 процесорах: S₁₀₀ = 1/(0.1 + 0.9/100) = 1/0.109 ≈ 9.17x. Приклад 2: f = 0.5 (50% послідовний код). S∞ = 2x. S₁₀₀ = 1/(0.5 + 0.5/100) ≈ 1.98x. Навіть 100 процесорів дають лише 2x!" },

      { heading: "Ефект Амдала та його обмеження" },
      { text: "Ефект Амдала: навіть невелика послідовна частина суттєво обмежує масштабованість. Це є фундаментальним обмеженням паралельних обчислень, яке стимулює: мінімізацію послідовних секцій; розпаралелювання якомога більшої частини алгоритму; вибір задач з природно великою паралельною часткою." },

      { heading: "Реалізація та вимірювання в Java" },
      { code: `public class AmdahlBenchmark {
    public static void measureSpeedup(double serialFraction, int maxThreads) {
        int N = 10_000_000;
        
        // Симулюємо задачу з фіксованою послідовною частиною
        long serialWork = (long)(N * serialFraction);
        long parallelWork = N - serialWork;
        
        // Послідовний час:
        long t1 = System.nanoTime();
        computeSerial(serialWork);
        computeParallel(parallelWork, 1);
        long T1 = System.nanoTime() - t1;
        
        System.out.printf("%-10s %-15s %-12s %-12s%n", 
            "Потоки", "Час (мс)", "Прискорення", "Ефективність");
        
        for (int p = 1; p <= maxThreads; p *= 2) {
            long tp = System.nanoTime();
            computeSerial(serialWork);
            computeParallel(parallelWork, p);
            long Tp = System.nanoTime() - tp;
            
            double speedup = (double) T1 / Tp;
            double efficiency = speedup / p;
            double amdahlLimit = 1.0 / (serialFraction + (1-serialFraction)/p);
            
            System.out.printf("%-10d %-15.2f %-12.3f %-12.3f (Амдал: %.3f)%n",
                p, Tp/1e6, speedup, efficiency, amdahlLimit);
        }
    }
}` },

      { heading: "Закон Густафсона — альтернативний погляд" },
      { text: "Джон Густафсон (1988) запропонував альтернативне формулювання: якщо зі збільшенням p ми також збільшуємо розмір задачі, щоб загальний час залишався константним, то прискорення Sₚ = p + f(1-p) = p - f(p-1). Це більш оптимістичний погляд: прискорення може необмежено зростати зі збільшенням задачі. На практиці закон Густафсона краще описує реальні наукові обчислення, де розмір задачі зазвичай збільшується разом із обчислювальними ресурсами." }
    ]
  },
  {
    id: 33,
    title: "33. Оцінка ефективності паралельних обчислень",
    answer: [
      { heading: "Методологія профілювання" },
      { text: "Оцінка ефективності паралельних обчислень включає: теоретичний аналіз складності, емпіричні виміри та профілювання. Необхідно розрізняти CPU time (процесорний час, враховує всі ядра) та wall clock time (реальний час виконання). Java надає System.nanoTime() для точних вимірів. Виміри слід повторювати кілька разів (JVM warmup, JIT компіляція на перших ітераціях) та усереднювати." },

      { heading: "JMH — Java Microbenchmark Harness" },
      { code: `@BenchmarkMode(Mode.AverageTime)
@OutputTimeUnit(TimeUnit.MILLISECONDS)
@State(Scope.Thread)
@Warmup(iterations = 5)
@Measurement(iterations = 10)
public class MatrixMultiplyBenchmark {
    
    @Param({"100", "500", "1000"})
    int matrixSize;
    
    double[][] a, b;
    
    @Setup
    public void setup() {
        a = generateMatrix(matrixSize);
        b = generateMatrix(matrixSize);
    }
    
    @Benchmark
    public double[][] sequential() {
        return MatrixOps.multiplySeq(a, b);
    }
    
    @Benchmark
    public double[][] parallel4() {
        return MatrixOps.multiplyParallel(a, b, 4);
    }
    
    @Benchmark  
    public double[][] parallel8() {
        return MatrixOps.multiplyParallel(a, b, 8);
    }
}` },

      { heading: "Аналіз масштабованості" },
      { code: `public class ScalabilityAnalysis {
    public static void main(String[] args) throws Exception {
        int N = 1000;
        double[][] A = generateMatrix(N), B = generateMatrix(N);
        
        // Вимір послідовного часу
        long t1Start = System.nanoTime();
        multiplySeq(A, B);
        long T1 = System.nanoTime() - t1Start;
        
        System.out.println("Послідовний час: " + T1/1e6 + " мс");
        System.out.printf("%-8s %-12s %-12s %-12s%n", 
            "Потоки", "Час(мс)", "Прискорення", "Ефективність");
        
        int maxThreads = Runtime.getRuntime().availableProcessors();
        for (int p = 1; p <= maxThreads; p++) {
            long tpStart = System.nanoTime();
            multiplyParallel(A, B, p);
            long Tp = System.nanoTime() - tpStart;
            
            double Sp = (double) T1 / Tp;
            double Ep = Sp / p;
            System.out.printf("%-8d %-12.2f %-12.3f %-12.2f%%%n",
                p, Tp/1e6, Sp, Ep*100);
        }
    }
}` },

      { heading: "Джерела неефективності" },
      { text: "1. Накладні витрати синхронізації: кожен synchronized, lock, CAS операція — додатковий час. При багатьох дрібних критичних секціях це може домінувати. 2. Cache coherence (когерентність кешу): коли кілька ядер модифікують дані в одній cache line (false sharing), кожна зміна потребує оновлення кешів усіх ядер через протокол MESI — надзвичайно дорого. Вирішення: padding між даними різних потоків (@Contended у Java 8+). 3. Load imbalance (нерівномірне навантаження): якщо деякі потоки виконують більше роботи, інші простоюють у очікуванні бар'єру. 4. Memory bandwidth: при N ядрах всі звертаються до спільної шини пам'яті — пропускна здатність обмежена. 5. NUMA ефекти: на NUMA-машинах доступ до 'чужої' пам'яті дорожчий." },

      { heading: "False sharing та @Contended" },
      { code: `// ПРОБЛЕМА: обидва поля в одній cache line (64 байти)
class BadCounter {
    long counter1; // threads 1-4 модифікують
    long counter2; // threads 5-8 модифікують
    // кожна зміна інвалідує кеш всіх ядер!
}

// ВИРІШЕННЯ: @Contended (JVM flag: -XX:-RestrictContended)
class GoodCounter {
    @Contended long counter1;
    @Contended long counter2;
}

// Або ручний padding:
class PaddedCounter {
    long counter;
    long p1, p2, p3, p4, p5, p6, p7; // 7*8=56 байт padding
}` }
    ]
  },
  {
    id: 35,
    title: "35. Архітектура розподілених систем. Таксономія Фліна",
    answer: [
      { heading: "Таксономія Фліна (Flynn's Taxonomy, 1966)" },
      { text: "Майкл Флін запропонував класифікацію архітектур комп'ютерів на основі кількості потоків інструкцій (Instruction Streams, IS) та потоків даних (Data Streams, DS): SISD, SIMD, MISD, MIMD." },

      { heading: "SISD — Single Instruction, Single Data" },
      { text: "Класична фон Нейманівська архітектура: один потік інструкцій обробляє один потік даних. Приклад: послідовна програма на однопроцесорному комп'ютері. Усі класичні алгоритми написані для SISD. Нині використовується лише в embedded системах та мікроконтролерах." },

      { heading: "SIMD — Single Instruction, Multiple Data" },
      { text: "Одна інструкція одночасно обробляє кілька елементів даних. Приклад: SSE/AVX інструкції процесорів Intel/AMD (один регістр 256 біт = 8 float одночасно), GPU (тисячі ядер виконують одну shader-програму над мільйонами пікселів). SIMD ідеальний для матричних операцій, обробки сигналів, машинного навчання. Java Vector API (JEP 338) надає доступ до SIMD інструкцій." },

      { heading: "MISD — Multiple Instruction, Single Data" },
      { text: "Кілька процесорів обробляють одні й ті самі дані різними інструкціями. Теоретична категорія, рідко зустрічається на практиці. Приклад: конвеєрна обробка, де кожна стадія виконує різну операцію над тим самим потоком даних; fault-tolerant системи, де кілька різних алгоритмів обробляють ті самі дані для перевірки результатів (авіаційні системи)." },

      { heading: "MIMD — Multiple Instruction, Multiple Data" },
      { text: "Найпоширеніша категорія сучасних паралельних систем: кілька процесорів виконують різні програми над різними даними. Поділяється на: SMP (Symmetric Multiprocessing) — спільна пам'ять, UMA (Uniform Memory Access): всі процесори мають однаковий доступ до всієї пам'яті. Приклад: багатоядерні CPU. NUMA (Non-Uniform Memory Access): доступ до «своєї» пам'яті швидший. Приклад: багатопроцесорні сервери. Cluster/Grid: розподілена пам'ять, зв'язок через мережу. Приклад: HPC кластери з MPI." },

      { heading: "Архітектура розподілених систем" },
      { text: "Розподілена система — сукупність незалежних вузлів, що виглядає для користувача як єдина когерентна система (Таненбаум). Ключові характеристики: вузли не мають спільної пам'яті; комунікація через передачу повідомлень; часткові відмови (деякі вузли можуть відмовити, поки інші продовжують роботу); конкурентність; відсутність глобального годинника." },
      { text: "Архітектурні патерни розподілених систем: клієнт-сервер (один сервер обслуговує багато клієнтів); peer-to-peer (кожен вузол є одночасно клієнтом і сервером); publish-subscribe (видавці надсилають повідомлення, підписники отримують); мікросервіси (система розбита на малі незалежні сервіси, кожен у власному процесі/контейнері)." },

      { heading: "CAP теорема" },
      { text: "Eric Brewer (2000): розподілена система може одночасно гарантувати лише 2 з 3 властивостей. Consistency (консистентність): кожен запит отримує або актуальну відповідь, або помилку. Availability (доступність): кожен запит отримує відповідь (але не обов'язково актуальну). Partition tolerance (стійкість до розділення): система продовжує працювати при мережевому розділенні. CA-системи (без partition tolerance): традиційні RDBMS. CP-системи: ZooKeeper, HBase, MongoDB. AP-системи: Cassandra, CouchDB, DNS." }
    ]
  },
  {
    id: 37,
    title: "37. Стандарти та технології обміну повідомленнями в розподілених системах",
    answer: [
      { heading: "Огляд стандартів обміну повідомленнями" },
      { text: "Комунікація між вузлами розподіленої системи є ключовою проблемою. Виділяють кілька категорій: низькорівневі MPI стандарти для HPC кластерів; message brokers (брокери повідомлень) для enterprise систем; HTTP/REST для веб-сервісів; gRPC для мікросервісів." },

      { heading: "MPI (Message Passing Interface)" },
      { text: "MPI — стандарт для передачі повідомлень у паралельних та розподілених обчисленнях. Версія MPI-4.1 (2023) є актуальною. Ключові властивості: point-to-point комунікації (Send/Recv); колективні операції (Broadcast, Scatter, Gather, Reduce, AllReduce); підтримка топологій (картезіанська, граф); однобічні комунікації (MPI_Put, MPI_Get); не-блокуючі операції (MPI_Isend, MPI_Irecv). OpenMPI та MPICH — найпоширеніші реалізації. MPJ Express — реалізація MPI для Java." },

      { heading: "AMQP та Apache RabbitMQ" },
      { text: "AMQP (Advanced Message Queuing Protocol) — відкритий стандарт для обміну повідомленнями між застосунками. RabbitMQ реалізує AMQP та додає розширення. Концепції AMQP: Producer надсилає повідомлення до Exchange; Exchange маршрутизує повідомлення до Queue за правилами; Consumer читає з Queue. Режими маршрутизації: direct (за routing key); topic (за шаблоном); fanout (до всіх черг); headers. Використовується для: декуплінгу мікросервісів; асинхронної обробки задач; балансування навантаження." },

      { heading: "Apache Kafka — розподілений журнал подій" },
      { text: "Kafka — розподілена платформа потокової обробки даних. Архітектура: Topic розбитий на Partition; кожен Partition — впорядкована незмінна послідовність записів; Consumer Group — кілька споживачів читають різні Partition паралельно; Replication — кожен Partition реплікується на кількох брокерах. Гарантії: at-least-once (принаймні раз); at-most-once (щонайбільше раз); exactly-once (рівно раз, з транзакціями). Kafka використовується для: event sourcing; CDC (Change Data Capture); real-time analytics; мікросервісної шини подій." },

      { heading: "gRPC — сучасний RPC фреймворк" },
      { text: "gRPC (Google Remote Procedure Call) — відкритий фреймворк від Google для ефективного RPC. Особливості: Protocol Buffers для серіалізації (10x швидше за JSON); HTTP/2 транспорт (multiplexing, header compression, server push); 4 типи з'єднань: unary, server streaming, client streaming, bidirectional streaming; генерація коду для Java, Go, Python, C++, та ін. gRPC використовується у мікросервісах де важлива продуктивність (Kubernetes, etcd, TensorFlow Serving)." },

      { heading: "REST (Representational State Transfer)" },
      { text: "REST — архітектурний стиль для розподілених гіпермедіа систем (Roy Fielding, 2000). RESTful API використовує HTTP методи: GET (читання), POST (створення), PUT/PATCH (оновлення), DELETE (видалення). Принципи REST: stateless (сервер не зберігає стан між запитами); uniform interface (уніфікований інтерфейс); cacheable; client-server; layered system. В Java: Spring Boot + Spring Web, JAX-RS (Jersey, RESTEasy)." },

      { heading: "WebSocket — двостороннє з'єднання" },
      { text: "WebSocket забезпечує повнодуплексний канал зв'язку поверх одного TCP-з'єднання. На відміну від HTTP, з'єднання залишається відкритим — обидві сторони можуть надсилати повідомлення будь-коли. Використовується для: чатів; real-time dashboards; онлайн-ігор; push-notifications. Java: Spring WebSocket, Jetty WebSocket API." }
    ]
  },
  {
    id: 39,
    title: "39. Методи обміну повідомленнями стандарту MPI один до одного та їх застосування",
    answer: [
      { heading: "Концепція P2P комунікацій у MPI" },
      { text: "Point-to-point (P2P) комунікації в MPI — це передача повідомлень між двома конкретними процесами: відправником (sender) та отримувачем (receiver). Кожен процес ідентифікується своїм рангом (rank) у комунікаторі (communicator). MPI_COMM_WORLD — стандартний комунікатор, що включає всі процеси." },

      { heading: "MPI_Send та MPI_Recv — блокуючі операції" },
      { text: "MPI_Send(buf, count, datatype, dest, tag, comm) — надсилає повідомлення. Блокується до тих пір, поки буфер можна безпечно повторно використати (або повідомлення передане, або скопійоване до системного буфера). MPI_Recv(buf, count, datatype, source, tag, comm, status) — отримує повідомлення. Блокується до отримання повного повідомлення." },
      { code: `// MPJ Express (Java MPI):
import mpi.*;

public class P2PExample {
    public static void main(String[] args) throws Exception {
        MPI.Init(args);
        int rank = MPI.COMM_WORLD.Rank();
        int size = MPI.COMM_WORLD.Size();
        
        if (rank == 0) {
            double[] data = {1.0, 2.0, 3.0, 4.0};
            MPI.COMM_WORLD.Send(data, 0, 4, MPI.DOUBLE, 1, 0);
            System.out.println("Процес 0 надіслав дані");
        } else if (rank == 1) {
            double[] buffer = new double[4];
            Status status = MPI.COMM_WORLD.Recv(
                buffer, 0, 4, MPI.DOUBLE, 0, 0);
            System.out.println("Процес 1 отримав: " + Arrays.toString(buffer));
            System.out.println("Кількість елементів: " + 
                status.Get_count(MPI.DOUBLE));
        }
        MPI.Finalize();
    }
}` },

      { heading: "Неблокуючі операції: MPI_Isend та MPI_Irecv" },
      { text: "Неблокуючі (non-blocking) операції повертають керування одразу, не чекаючи завершення передачі. Операція виконується у фоні. MPI_Request — дескриптор незавершеної операції. MPI_Wait/MPI_Test — очікування або перевірка завершення операції." },
      { code: `// Неблокуючий обмін (накладання обчислень і комунікацій):
double[] sendBuf = computeMyData();
double[] recvBuf = new double[N];

// Починаємо асинхронно надсилати та отримувати:
Request sendReq = MPI.COMM_WORLD.Isend(sendBuf, 0, N, MPI.DOUBLE, dest, 0);
Request recvReq = MPI.COMM_WORLD.Irecv(recvBuf, 0, N, MPI.DOUBLE, source, 0);

// Поки дані передаються — виконуємо корисну роботу:
computeInnerCells(); // обчислення, що не потребують сусідніх даних

// Тепер чекаємо завершення комунікацій:
sendReq.Wait();
recvReq.Wait();

// Тепер можна використовувати recvBuf:
computeBoundaryCells(recvBuf);` },

      { heading: "MPI_Sendrecv — атомарний обмін" },
      { text: "MPI_Sendrecv дозволяє одночасно надсилати одному процесу та отримувати від іншого. Запобігає deadlock при кільцевих обмінах (типова ситуація у стрічковому алгоритмі та алгоритмі Кеннона). MPI.COMM_WORLD.Sendrecv(sendBuf, 0, count, MPI.DOUBLE, dest, tag, recvBuf, 0, count, MPI.DOUBLE, source, tag)." },

      { heading: "Застосування для паралельного множення матриць (стрічковий алгоритм)" },
      { code: `// Псевдокод стрічкового алгоритму з MPI:
// 1. Scatter рядків матриці A між процесами
MPI.COMM_WORLD.Scatter(A_flat, rowsPerProc*N, MPI.DOUBLE,
    localA, rowsPerProc*N, MPI.DOUBLE, 0);

// 2. Broadcast матриці B всім процесам
MPI.COMM_WORLD.Bcast(B_flat, N*N, MPI.DOUBLE, 0);

// 3. Кожен процес обчислює свої рядки C:
for (int i = 0; i < rowsPerProc; i++)
    for (int j = 0; j < N; j++)
        for (int k = 0; k < N; k++)
            localC[i][j] += localA[i][k] * B[k][j];

// 4. Gather результатів на процесі 0:
MPI.COMM_WORLD.Gather(localC_flat, rowsPerProc*N, MPI.DOUBLE,
    C_flat, rowsPerProc*N, MPI.DOUBLE, 0);` },

      { heading: "Теги повідомлень та комунікатори" },
      { text: "Тег (tag) дозволяє розрізняти різні типи повідомлень між одними й тими самими процесами. MPI_ANY_TAG та MPI_ANY_SOURCE — wildcards для отримання будь-якого повідомлення. Комунікатори дозволяють ізолювати підгрупи процесів: MPI_Comm_split розбиває MPI_COMM_WORLD на кілька підкомунікаторів. Це корисно для ієрархічних алгоритмів." }
    ]
  },
  {
    id: 41,
    title: "41. OpenMPI: особливості побудови програми та основні методи",
    answer: [
      { heading: "OpenMPI — реалізація стандарту MPI" },
      { text: "OpenMPI — найпопулярніша реалізація стандарту MPI з відкритим кодом, розроблена консорціумом університетів та компаній (Indiana University, Tennessee, Los Alamos та ін.). Особливості: підтримка MPI-3.1/MPI-4.0; інтеграція з SLURM/PBS планувальниками; підтримка InfiniBand, Ethernet, Shared Memory; оптимізовані колективні операції." },

      { heading: "Структура MPI-програми на Java (MPJ Express)" },
      { code: `import mpi.*;
import java.util.Arrays;

public class OpenMPIExample {
    public static void main(String[] args) throws MPIException {
        // Ініціалізація MPI оточення
        MPI.Init(args);
        
        // Отримання базової інформації:
        int rank = MPI.COMM_WORLD.Rank();   // ранг цього процесу (0..size-1)
        int size = MPI.COMM_WORLD.Size();   // загальна кількість процесів
        String procName = MPI.Get_processor_name(); // ім'я вузла
        
        System.out.printf("Процес %d з %d на %s%n", 
            rank, size, procName);
        
        // ... основна логіка програми ...
        
        // Обов'язкова фіналізація:
        MPI.Finalize();
    }
}` },

      { heading: "Колективні операції MPI" },
      { text: "Колективні операції залучають ВСІ процеси в комунікаторі. MPI_Barrier — синхронізація всіх процесів (всі чекають, поки всі досягнуть цієї точки). MPI_Bcast — один процес надсилає дані всім іншим. MPI_Scatter — root-процес розсилає різні частини масиву різним процесам. MPI_Gather — всі процеси надсилають дані root-процесу (зворотній до Scatter). MPI_Allgather — аналог Gather, але результат отримують усі процеси. MPI_Reduce — всі процеси надсилають дані, root отримує результат редукції (SUM, MAX, MIN тощо). MPI_AllReduce — аналог Reduce, але результат отримують усі." },
      { code: `// Колективні операції (MPJ Express):

// Broadcast: процес 0 надсилає matrix B всім:
MPI.COMM_WORLD.Bcast(B, 0, N*N, MPI.DOUBLE, 0);

// Scatter: розподіл рядків матриці A:
int rowsPerProcess = N / size;
double[] localA = new double[rowsPerProcess * N];
MPI.COMM_WORLD.Scatter(A, 0, rowsPerProcess*N, MPI.DOUBLE,
                       localA, 0, rowsPerProcess*N, MPI.DOUBLE, 0);

// AllReduce: сума часткових результатів (наприклад, перевірка):
double[] localSum = {computeLocalSum()};
double[] globalSum = new double[1];
MPI.COMM_WORLD.Allreduce(localSum, 0, globalSum, 0, 1, 
                         MPI.DOUBLE, MPI.SUM);

// Gather: збір результатів:
double[] localC = computeMyRows();
double[] C = (rank == 0) ? new double[N*N] : null;
MPI.COMM_WORLD.Gather(localC, 0, rowsPerProcess*N, MPI.DOUBLE,
                      C, 0, rowsPerProcess*N, MPI.DOUBLE, 0);` },

      { heading: "MPI_Barrier та синхронізація" },
      { code: `// Синхронізація всіх процесів:
MPI.COMM_WORLD.Barrier();
// Всі процеси чекають тут, поки всі дійдуть до цієї точки

// Вимірювання часу (правильний спосіб з MPI):
MPI.COMM_WORLD.Barrier(); // синхронізуємось перед вимірюванням
double startTime = MPI.Wtime(); // час у секундах (high resolution)

// ... виконання алгоритму ...

MPI.COMM_WORLD.Barrier(); // чекаємо всіх
double endTime = MPI.Wtime();
double elapsed = endTime - startTime;

// Знаходимо максимальний час (критичний шлях):
double[] localTime = {elapsed};
double[] maxTime = new double[1];
MPI.COMM_WORLD.Reduce(localTime, 0, maxTime, 0, 1, 
                      MPI.DOUBLE, MPI.MAX, 0);
if (rank == 0) System.out.printf("Час виконання: %.3f сек%n", maxTime[0]);` },

      { heading: "Запуск MPI-програми" },
      { code: `# Компіляція та запуск з OpenMPI:
javac -cp mpj.jar MyMPIProgram.java
mpirun -np 8 java -cp .:mpj.jar MyMPIProgram  # 8 процесів на локальній машині

# На кластері (SLURM):
# sbatch job.sh
# job.sh:
# #!/bin/bash
# #SBATCH -N 4          # 4 вузли
# #SBATCH --ntasks=32   # 32 MPI процеси
# mpirun java -cp .:mpj.jar MyMPIProgram

# З hostfile (список вузлів):
# mpirun -np 16 --hostfile hosts.txt java MyMPIProgram` }
    ]
  },
  {
    id: 43,
    title: "43. Алгоритми паралельного сортування",
    answer: [
      { heading: "Паралельне сортування злиттям (Parallel Merge Sort)" },
      { text: "Merge Sort природно піддається розпаралелюванню: масив ділиться на половини, кожна половина сортується паралельно (ForkJoin), після чого результати зливаються. Обмеження: фаза злиття є послідовною — критичний шлях O(n) навіть при нескінченній кількості потоків. Покращення: паралельне злиття (parallel merge) розбиває злиття на незалежні частини." },
      { code: `public class ParallelMergeSort extends RecursiveAction {
    private final int[] array;
    private final int left, right;
    static final int THRESHOLD = 10_000;
    
    public ParallelMergeSort(int[] array, int left, int right) {
        this.array = array; this.left = left; this.right = right;
    }
    
    @Override
    protected void compute() {
        if (right - left <= THRESHOLD) {
            Arrays.sort(array, left, right + 1);
            return;
        }
        int mid = (left + right) / 2;
        ParallelMergeSort leftTask = new ParallelMergeSort(array, left, mid);
        ParallelMergeSort rightTask = new ParallelMergeSort(array, mid + 1, right);
        invokeAll(leftTask, rightTask); // паралельне рекурсивне сортування
        merge(array, left, mid, right); // послідовне злиття
    }
    
    private void merge(int[] arr, int left, int mid, int right) {
        int[] temp = Arrays.copyOfRange(arr, left, right + 1);
        int i = 0, j = mid - left + 1, k = left;
        while (i <= mid - left && j <= right - left)
            arr[k++] = (temp[i] <= temp[j]) ? temp[i++] : temp[j++];
        while (i <= mid - left) arr[k++] = temp[i++];
        while (j <= right - left) arr[k++] = temp[j++];
    }
}` },

      { heading: "Бітонне сортування (Bitonic Sort)" },
      { text: "Бітонне сортування — алгоритм паралельного сортування на основі мережі сортування (sorting network). Послідовність операцій compare-and-swap фіксована та не залежить від вхідних даних — ідеально для GPU та FPGA. Розмір масиву має бути степенем 2. Складність: O(log²n) паралельних кроків, кожен крок містить n/2 незалежних порівнянь." },
      { code: `public class BitonicSort {
    // Паралельна реалізація з ForkJoin:
    public static void bitonicSort(int[] arr) {
        ForkJoinPool pool = ForkJoinPool.commonPool();
        int n = arr.length; // має бути степенем 2
        
        for (int k = 2; k <= n; k *= 2) {
            for (int j = k / 2; j >= 1; j /= 2) {
                final int jFinal = j, kFinal = k;
                // Всі пари (i, i XOR j) незалежні — виконуємо паралельно:
                pool.invoke(new RecursiveAction() {
                    protected void compute() {
                        List<RecursiveAction> tasks = new ArrayList<>();
                        for (int i = 0; i < n; i++) {
                            final int iFinal = i;
                            int l = i ^ jFinal;
                            if (l > i) {
                                boolean ascending = ((i & kFinal) == 0);
                                tasks.add(new RecursiveAction() {
                                    protected void compute() {
                                        if ((arr[iFinal] > arr[l]) == ascending) {
                                            int tmp = arr[iFinal];
                                            arr[iFinal] = arr[l];
                                            arr[l] = tmp;
                                        }
                                    }
                                });
                            }
                        }
                        invokeAll(tasks);
                    }
                });
            }
        }
    }
}` },

      { heading: "Паралельний Quicksort" },
      { text: "Паралельний QuickSort: після вибору pivot та partition масив ділиться на два незалежних підмасиви, які можна сортувати паралельно. Проблема: якість pivot суттєво впливає на балансування. Медіана трьох або випадковий pivot мінімізують ризик вироджених випадків. Java Arrays.parallelSort() використовує паралельний merge sort (гарантований O(n log n))." },

      { heading: "Паралельне сортування в Java 8+" },
      { code: `int[] array = new int[1_000_000];
// ... заповнення ...

// Arrays.parallelSort — вбудований паралельний merge sort:
Arrays.parallelSort(array); // використовує ForkJoinPool.commonPool()
Arrays.parallelSort(array, 0, 500_000); // сортування діапазону

// Для об'єктів:
String[] strings = {"banana", "apple", "cherry"};
Arrays.parallelSort(strings, Comparator.comparingInt(String::length));

// Паралельний sort через Stream:
int[] sorted = IntStream.range(0, 1_000_000)
    .parallel()
    .map(i -> array[i])
    .sorted()
    .toArray();` },

      { heading: "Sample Sort — для розподілених систем (MPI)" },
      { text: "Sample Sort ефективний для розподіленого сортування на кластерах: кожен процес сортує свою частину; всі процеси спільно вибирають p-1 зразків (samples) для визначення меж бакетів; дані перерозподіляються між процесами (alltoall); кожен процес сортує отриманий бакет. Результат: глобально відсортований масив розподілений між процесами." }
    ]
  },
  {
    id: 45,
    title: "45. Оцінювання продуктивності паралельних алгоритмів в розподілених системах",
    answer: [
      { heading: "Особливості розподілених систем" },
      { text: "На відміну від систем зі спільною пам'яттю, у розподілених системах комунікаційні витрати є домінуючим фактором продуктивності. Кожна передача повідомлень між вузлами включає: затримку (latency) α — фіксована вартість ініціалізації передачі (мікросекунди-мілісекунди); пропускну здатність (bandwidth) β — швидкість передачі даних (GB/s); час передачі n байт: T_comm = α + n/β." },

      { heading: "Модель BSP (Bulk Synchronous Parallel)" },
      { text: "BSP модель (Valiant, 1990) описує паралельні обчислення як послідовність супер-кроків (supersteps). Кожен супер-крок складається з: обчислювальної фази (кожен процес виконує локальні обчислення); комунікаційної фази (обмін повідомленнями між процесами); бар'єрної синхронізації (всі чекають завершення комунікацій). Вартість супер-кроку: max(w_i) + max(h_i) * g + L, де w_i — обчислювальна вартість процесу i, h_i — максимальна кількість повідомлень процесу i, g — відношення комунікаційної пропускної здатності до обчислювальної, L — вартість бар'єру." },

      { heading: "Ізоефективність для розподілених систем" },
      { code: `// Розрахунок ізоефективності для стрічкового алгоритму множення матриць:
// Обчислювальна вартість: T_comp = 2 * N^3 / p
// Комунікаційна вартість (Bcast B + Scatter A + Gather C):
//   T_comm = α * log(p) + β * N^2  (broadcast B)
//          + α + β * N^2/p          (scatter A)
//          + α + β * N^2/p          (gather C)
//   ≈ β * N^2 при великих N

// Ефективність E:
// E = T_seq / (p * T_par) = 2N^3 / (p * (2N^3/p + β*N^2))
//   = 1 / (1 + β*p/(2N))

// Для E = 0.9: β*p/(2N) = 1/9 → N = 4.5*β*p
// Тобто розмір задачі N має зростати лінійно з p для const ефективності
public static double efficiency(int N, int p, double beta) {
    double tComp = 2.0 * N * N * N / p;
    double tComm = beta * N * N; // спрощено
    return tComp / (tComp + tComm);
}` },

      { heading: "Метрика паралельного часу з урахуванням комунікацій" },
      { text: "Для алгоритму з MPI загальний час виконання: T_par = T_comp/p + T_comm. Де T_comp — обчислювальна складність, T_comm — комунікаційні витрати. Прискорення з урахуванням комунікацій: S_p = T_seq / T_par = T_seq / (T_comp/p + T_comm). При великому T_comm прискорення може бути менше 1 — паралельна версія повільніша за послідовну! Це стається при малих задачах або великій кількості процесорів." },

      { heading: "Вимірювання в MPI" },
      { code: `// Точне вимірювання часу MPI:
MPI.COMM_WORLD.Barrier(); // синхронізація перед стартом
double t_start = MPI.Wtime();

// Виконання алгоритму
runAlgorithm();

MPI.COMM_WORLD.Barrier(); // чекаємо завершення всіх
double t_end = MPI.Wtime();
double local_time = t_end - t_start;

// Збір статистики по всіх процесах:
double[] all_times = (rank == 0) ? new double[size] : null;
MPI.COMM_WORLD.Gather(
    new double[]{local_time}, 0, 1, MPI.DOUBLE,
    all_times, 0, 1, MPI.DOUBLE, 0
);

if (rank == 0) {
    double max_time = Arrays.stream(all_times).max().getAsDouble();
    double avg_time = Arrays.stream(all_times).average().getAsDouble();
    System.out.printf("Макс час: %.4f, Середній: %.4f, Незбаланс: %.1f%%%n",
        max_time, avg_time, (max_time/avg_time - 1) * 100);
    // Незбалансованість = (макс - середнє) / середнє * 100%
}` },

      { heading: "Профілювання MPI програм" },
      { text: "Інструменти профілювання MPI: TAU (Tuning and Analysis Utilities) — збирає детальну статистику по кожній MPI операції; Intel VTune Profiler — профілювання на рівні апаратних лічильників; MPE (MPI Parallel Environment) — генерує трасувальні логи для Jumpshot; Score-P/Scalasca — аналіз масштабованості на суперкомп'ютерах. Ключові метрики: MPI time (частка часу у MPI операціях); load imbalance (дисбаланс навантаження між процесами); communication volume (обсяг переданих даних)." }
    ]
  },
  {
    id: 47,
    title: "47. Грід-технології. Поняття спільного віртуального ресурсу",
    answer: [
      { heading: "Що таке Grid Computing" },
      { text: "Грід-обчислення (Grid Computing) — парадигма розподілених обчислень, що об'єднує комп'ютерні ресурси різних організацій через інтернет для спільного вирішення великих задач. Термін запозичено з аналогії з електромережею (power grid): так само як електрична мережа надає електрику 'за потребою', обчислювальний грід надає обчислювальні ресурси 'за потребою'. Основоположники концепції: Ian Foster та Carl Kesselman ('The Grid: Blueprint for a New Computing Infrastructure', 1999)." },

      { heading: "Відмінності Grid від Cluster та Cloud" },
      { text: "Кластер (Cluster): гомогенна група тісно пов'язаних комп'ютерів в одному місці, під єдиним управлінням. Грід (Grid): гетерогенна, географічно розподілена інфраструктура, що об'єднує ресурси різних організацій. Хмара (Cloud): комерційна платформа надання обчислювальних ресурсів за моделлю IaaS/PaaS/SaaS. Ключова відмінність грід: ресурси належать різним організаціям та перетинають адміністративні межі — це вимагає спеціальних механізмів безпеки, авторизації та управління." },

      { heading: "Поняття спільного віртуального ресурсу (Virtual Organization)" },
      { text: "Ключова концепція грід-технологій — Віртуальна Організація (Virtual Organization, VO). VO — це динамічна, тимчасова група людей та установ, що поділяють ресурси для досягнення спільної мети. Особливості VO: правила та умови доступу до ресурсів визначаються угодами між організаціями; членство динамічне — ресурси та учасники можуть приєднуватись та покидати; VO може об'єднувати ресурси університетів, лабораторій, компаній з різних країн." },
      { text: "Приклад: CERN (ЦЕРН) організував LHC Computing Grid (WLCG) — грід для обробки даних Великого адронного колайдера. 170 обчислювальних центрів у 42 країнах, близько 1 мільйон обчислювальних ядер, понад 1 ексабайт (10^18 байт) зберігання. Данні з LHC передаються у Tier-1 центри (11 великих обчислювальних центрів), звідти — у Tier-2 центри (університети), що становить VO фізиків ядра." },

      { heading: "Архітектура OGSA (Open Grid Services Architecture)" },
      { text: "OGSA — стандартна архітектура для грід-систем, розроблена Global Grid Forum. OGSA базується на веб-сервісах та визначає стандартні інтерфейси для: управління ресурсами (Resource Management); управління задачами (Job Management); інформаційних сервісів (Information Services); управління даними (Data Management); безпеки (Security). WSRF (Web Services Resource Framework) — специфікація для реалізації OGSA на основі веб-сервісів." },

      { heading: "Globus Toolkit" },
      { text: "Globus Toolkit — найбільш поширений відкритий набір інструментів для побудови грід-систем. Компоненти: GRAM (Grid Resource Allocation and Management) — відправлення задач на виконання; GridFTP — високопродуктивне передавання файлів між вузлами; GSI (Grid Security Infrastructure) — аутентифікація через X.509 сертифікати; MDS (Monitoring and Discovery Service) — виявлення та моніторинг ресурсів. GridFTP є вдосконаленням FTP для грід: підтримує паралельні потоки, часткову відновлюваність, третьохсторонню передачу даних." },

      { heading: "BOINC — грід для наукових розрахунків" },
      { text: "BOINC (Berkeley Open Infrastructure for Network Computing) — платформа для добровільних distributed computing проектів. Доброволі встановлюють клієнт, що виконує наукові розрахунки, коли комп'ютер вільний. Проекти: SETI@home (пошук позаземного інтелекту); Folding@home (згортання білків для дослідження хвороб); Einstein@Home (пошук гравітаційних хвиль); Rosetta@home (передбачення структури білків). BOINC — приклад грід, де ресурси надаються добровільно через інтернет — найбільша VO у світі." }
    ]
  },
  {
    id: 49,
    title: "49. Програмне забезпечення грід-технологій",
    answer: [
      { heading: "Рівні ПЗ грід-систем" },
      { text: "Програмне забезпечення грід-технологій організоване у шари (layers): апаратний рівень (hardware) — процесори, диски, мережа; рівень ОС та локального ПЗ — Linux, Windows, локальні системи управління ресурсами; рівень грід middleware — стандартизовані сервіси, що абстрагують апаратний рівень; рівень застосунків — наукові та інженерні програми." },

      { heading: "Globus Toolkit — компоненти" },
      { text: "Globus Toolkit (GT) є де-факто стандартом ПЗ грід. Детальніше про компоненти: GT4/GT5 GRAM — система управління та розподілу ресурсів. Надсилає RSL (Resource Specification Language) опис задачі до конкретного вузла або планувальника (PBS, SLURM, SGE). GT GridFTP: TCP-based протокол, оптимізований для Wide Area Networks. Підтримує: паралельні TCP потоки (до 64); часткове відновлення при перериванні; автентифікацію GSI; третьохсторонню передачу (FTP over FTP без копіювання на клієнт). GT MyProxy — сервер для делегування X.509 сертифікатів. Дозволяє довготривалим грід-задачам отримувати короткочасні проксі-сертифікати без участі користувача." },

      { heading: "UNICORE (Uniform Interface to Computing Resources)" },
      { text: "UNICORE — ще одне грід middleware, розроблене в Європі (Jülich Supercomputing Centre). Особливості: повністю Java-реалізація; архітектура клієнт-шлюз-сервер; інтеграція з SAML/X.509 для аутентифікації; підтримка JSDL (Job Submission Description Language). Широко використовується у EUDAT та PRACE (Partnership for Advanced Computing in Europe)." },

      { heading: "WLCG Middleware стек (LHC Computing Grid)" },
      { text: "gLite — спеціалізований грід middleware стек, розроблений для WLCG/EGEE (Enabling Grids for E-sciencE): WMS (Workload Management System) — розподіл задач між Tier-1/Tier-2 сайтами; LFC (LCG File Catalog) — каталог файлів: де розташовані репліки кожного файлу; FTS (File Transfer Service) — надійний сервіс передачі даних між сайтами; CREAM (Computing Resource Execution And Management) — локальний сервіс управління задачами; DPM (Disk Pool Manager) / dCache — розподілені системи зберігання даних." },

      { heading: "Сучасне грід-ПЗ та Cloud Gateway" },
      { text: "SciGaP (Science Gateways Platform) та Apache Airavata надають веб-інтерфейси для запуску HPC/Grid задач без необхідності знати деталі middleware. Open Science Grid (OSG, США) та EGI (European Grid Infrastructure) є активними грід-інфраструктурами. Тенденція: інтеграція грід та хмарних ресурсів через Cloud Bursting — коли локальні грід ресурси перевантажені, задачі автоматично відправляються в хмару (AWS, Azure, GCP)." },

      { heading: "Аутентифікація та безпека в грід" },
      { text: "Ключова проблема грід — безпека при перетині адміністративних меж. GSI (Grid Security Infrastructure): базується на X.509 PKI; кожен користувач/ресурс має сертифікат, підписаний довіреним CA (Certificate Authority); proxy certificates дозволяють делегувати право виступати від імені користувача. VOMS (Virtual Organization Membership Service) — авторизація: видає VOMS proxy certificates з атрибутами членства у VO, ролями та групами. XACML (eXtensible Access Control Markup Language) — стандарт для вираження політик авторизації." },

      { heading: "Моніторинг грід-ресурсів" },
      { text: "MDS (Monitoring and Discovery Service) в Globus Toolkit та BDII (Berkeley Database Information Index) в gLite збирають інформацію про стан ресурсів. GLUE Schema (Grid Laboratory Uniform Environment) — стандартна схема для опису грід-ресурсів: обчислювальні елементи (CE), елементи зберігання (SE), характеристики вузлів. Ganglia та Nagios — типові інструменти для моніторингу стану вузлів. Graphite/Grafana — для відображення метрик у реальному часі." }
    ]
  }
];

function makeHeading2(text) {
  return new Paragraph({
    heading: HeadingLevel.HEADING_2,
    children: [new TextRun({ text, bold: true, size: 26, font: "Arial" })],
    spacing: { before: 200, after: 100 }
  });
}

function makeHeading3(text) {
  return new Paragraph({
    children: [new TextRun({ text, bold: true, size: 24, font: "Arial", color: "1F4E79" })],
    spacing: { before: 160, after: 60 }
  });
}

function makeBody(text) {
  return new Paragraph({
    children: [new TextRun({ text, size: 22, font: "Arial" })],
    spacing: { before: 60, after: 60 },
    alignment: AlignmentType.JUSTIFIED
  });
}

function makeCode(code) {
  return new Paragraph({
    children: [new TextRun({ text: code, size: 18, font: "Courier New", color: "1A1A2E" })],
    spacing: { before: 60, after: 60 },
    shading: { fill: "F0F4F8" },
    indent: { left: 720 }
  });
}

function makeDivider() {
  return new Paragraph({
    children: [],
    border: { bottom: { style: BorderStyle.SINGLE, size: 6, color: "CCCCCC", space: 1 } },
    spacing: { before: 200, after: 200 }
  });
}

const sections_children = [];

// Title page
sections_children.push(
  new Paragraph({
    children: [new TextRun({ text: "Технологія паралельних обчислень", bold: true, size: 48, font: "Arial", color: "1F4E79" })],
    alignment: AlignmentType.CENTER,
    spacing: { before: 2000, after: 400 }
  }),
  new Paragraph({
    children: [new TextRun({ text: "Відповіді на екзаменаційні питання", size: 32, font: "Arial", color: "2E75B6" })],
    alignment: AlignmentType.CENTER,
    spacing: { before: 200, after: 200 }
  }),
  new Paragraph({
    children: [new TextRun({ text: "Мова програмування: Java", size: 26, font: "Arial", color: "555555" })],
    alignment: AlignmentType.CENTER,
    spacing: { before: 100, after: 4000 }
  })
);

// Questions
for (const q of questions) {
  // Page break before each question (except first)
  if (q !== questions[0]) {
    sections_children.push(new Paragraph({ children: [new PageBreak()] }));
  }
  
  // Question heading
  sections_children.push(
    new Paragraph({
      heading: HeadingLevel.HEADING_1,
      children: [new TextRun({ text: q.title, bold: true, size: 32, font: "Arial", color: "1F4E79" })],
      spacing: { before: 400, after: 300 }
    })
  );
  
  // Answer content
  for (const block of q.answer) {
    if (block.heading) {
      sections_children.push(makeHeading3(block.heading));
    } else if (block.text) {
      sections_children.push(makeBody(block.text));
    } else if (block.code) {
      // Split code by lines and add each line
      const lines = block.code.split('\n');
      for (const line of lines) {
        sections_children.push(makeCode(line));
      }
    }
  }
  
  sections_children.push(makeDivider());
}

const doc = new Document({
  styles: {
    default: {
      document: { run: { font: "Arial", size: 22 } }
    },
    paragraphStyles: [
      {
        id: "Heading1",
        name: "Heading 1",
        basedOn: "Normal",
        next: "Normal",
        quickFormat: true,
        run: { size: 32, bold: true, font: "Arial", color: "1F4E79" },
        paragraph: { spacing: { before: 400, after: 200 }, outlineLevel: 0 }
      },
      {
        id: "Heading2",
        name: "Heading 2",
        basedOn: "Normal",
        next: "Normal",
        quickFormat: true,
        run: { size: 26, bold: true, font: "Arial", color: "2E75B6" },
        paragraph: { spacing: { before: 200, after: 100 }, outlineLevel: 1 }
      }
    ]
  },
  sections: [{
    properties: {
      page: {
        size: { width: 11906, height: 16838 },
        margin: { top: 1134, right: 1134, bottom: 1134, left: 1134 }
      }
    },
    children: sections_children
  }]
});

Packer.toBuffer(doc).then(buffer => {
  fs.writeFileSync('./docs/uv2.docx', buffer);
}).catch(err => {
  console.error('Error:', err);
  process.exit(1);
});
